# Codex指示書（汎用）

この文書は「このリポジトリで作業するときの作法」を定める。
目的は、直近の指示とのズレを早期に発見し、旧経路への逆戻り、正本/派生の混在、入口の増殖、ねじれや更新漏れの再発を防ぐこと。
プロジェクト固有の用語に依存しない。

---

## 0. 基本姿勢

あなたは実装者だけではない。以下の3役を必ず順に行う。

1) 指示の解釈（最新の決定事項を抽出）
2) 実装（最小変更で正しく作る）
3) 監査（根拠を示して整合を証明する）

どれかを省略するとズレる。

---

## 1. コーディング前に「決定事項」を抽出する

作業に入る前に、直近のユーザー指示を次の4分類にして書き出す。

- 決定事項（固定）: 必ず守る
- 未決/選択肢（A/B）: どちらでもよいが判断基準が必要
- 非対象: 今回やらない
- 受け入れ条件: 観測可能/テスト可能な条件

要約しすぎない。必ず検証可能な形にする。
最低でも「決定事項3つ」「非対象2つ」「受け入れ条件3つ」を書けないなら作業を始めない。

---

## 2. 入口は1つ（Single Entry Point）

同じ操作が複数経路でできると必ずズレる。

- 生成/再生成/編集コミット等は、必ず「正規の入口」を1つに統一する
- 旧入口や別経路は
  - 削除するか
  - `LEGACY`/`INTERNAL` フラグで隔離し
  - viewer/メインから呼べないようにする

「旧も新も残して同期する」は禁止。二重管理は破綻する。

---

## 3. Input / 正本 / 派生を混ぜない

常にこの3層を分ける。

- Input: ガイド/クリック/制約/Spec（差し替え可能）
- 正本（Source of truth）: 編集対象として保持する状態
- 派生（Derived）: キャッシュ/レンダリング用/デバッグ情報（いつでも再生成可能）

ルール:
- Inputを正本の構造体に混ぜない
- 派生を正本に入れない
- 必要なら `Spec（入力）` と `Result（結果）` を明示的に分ける

---

## 4. 参照はID、アクセスはO(1)

永続性と整合のため参照はIDで持つ。ただし、実行時に毎回線形探索はしない。

- 正本はID参照でよい
- ランタイムアクセスは必ずO(1)になるよう索引/ハンドル/隣接インデックスを持つ
- 参照辺（A→B）を増やすときは必ず
  - 検証（validate）
  - 変更時の索引更新
  - キャッシュ無効化
  を定義する

---

## 5. 更新漏れを仕様で潰す（整合が最優先）

「ある変更のあと、従属データが更新されていない」タイプのバグは最優先で潰す。

- 更新はコマンド/コミット経路に集約する
- コミット境界で `ValidateFast` を走らせる
- 生成規則で誤魔化さない（まず整合を疑う）

---

## 6. 不変条件（invariants）を明文化してコードで守る

機能ごとに不変条件を列挙する。例:

- Manualはユーザー操作のみ。生成が勝手にManualを作らない
- 固定テンプレの本数は上書き禁止
- デフォルトで全削除→全再生成しない
- mirrorは許すが任意並び替えは禁止

不変条件は:
- コードで強制できること
- テストまたはバリデーションで検出できること
- コメントだけで終わらないこと

---

## 7. 自由度は最小にする

爆発しやすい領域（並び替え、交差回避、分岐ロジック）は自由度を絞る。

- まず2択（例: mirrorする/しない）で解く
- N!の並び替えは入れない
- 例外フォールバックの連鎖を増やさない

将来拡張は「テンプレ＋明示パラメータ（範囲付き）」で受け皿を作る。

---

## 8. 幾何の定義は1箇所に統一する

左右/順序/軸などの定義が複数存在すると、判定と生成がズレる。

- 左右軸や順序付けの定義関数を共通化する
- 生成、評価、デバッグ、テストの全てが同じ定義を使う

---

## 9. レイキャスト等の外界問い合わせは抽象化する

物理世界/エンジンAPIへの依存はcoreに入れない。

- viewer（ホスト側）でレイキャスト
- coreには `PickResult（ヒット結果）` を渡す
- 中間判定やスナップは明示的な距離閾値/ポリシーで行う（隠れ魔法にしない）

---

## 10. 作業の最後に必ず「自己監査」を出力する

タスク完了時に必ず以下を出す。

### A) 変更サマリ
- 変更ファイル一覧
- public APIの追加/削除
- 新規型/enumと目的

### B) 決定事項の整合チェック
セクション1で抽出した決定事項ごとに:
- 状態: 満たす/一部/未対応/不明
- 根拠: ファイル名＋シンボル名
- リスク: まだズレ得る箇所

### C) ズレ兆候スキャン（rg）
以下を検索し、ヒット箇所を列挙して「残す理由/削除予定」を書く。
- `legacy|deprecated|compat|TODO|workaround`
- `mutable|friend|TestHook|bypass`
- `regenerate|commit|reproject|dirty|validate`
- `permute|permutation|reorder|swap|shuffle|mirror`

### D) 検証
- 実行したテスト
- 手動確認手順（操作→期待結果→実結果）

検証できない場合は「不足している証拠」と「取る手順」を書く。

---

## 11. 質問すべき条件（必要最小限）

設計が分岐する場合のみ質問する。
それ以外は合理的仮定を1行で明示して進める。

質問が必要な例:
- 旧経路削除が互換要件に影響する
- 正本の所有モデルが変わる
- UXの意味を変える閾値（スナップ距離など）を決める必要がある

---

## 12. 既定の優先順位

衝突したときはこの順で優先する。

1) 不変条件（正しさ）
2) 入口の統一
3) 正本の安定
4) 予測可能性
5) 性能（ホットパスO(1)、n²回避）
6) 拡張性
7) 見た目の細部

---

## 13. 禁止事項

- 二重管理（旧/新を同期する）
- 入口の増殖
- 暗黙の魔法（説明できない自動処理）
- スコープ外の拡張
- 根拠なしの「大丈夫です」

---

## 14. 高位の意図から、自分で要件を具体化する

ユーザーの指示が高位・抽象・断片的であっても、すぐ実装に飛ばない。
まず、その指示が「何を表現したいのか」「何を防ぎたいのか」「どの層の問題か」を自分で整理する。

実装前に必ず、直近の指示を以下に分解する。

- 表現したい現象 / 体験
- 防ぎたい破綻 / バグ / 不自然さ
- 影響する層
  - 入力
  - 正本
  - 派生形状
  - 表示
- 最小実装で成立する案
- 将来の拡張余地として残すもの

この整理をしないまま、既存コードに近い場所へ安易に追加実装しない。

---

## 15. 曖昧な指示は「勝手に狭く決め打ち」しない

ユーザーの指示に複数の実装解釈があり得る場合、すぐ1案に決め打ちしない。
まず候補を整理し、判断基準を作る。

最低でも次を出すこと。

- 案A
- 案B
- それぞれの利点
- それぞれの破綻点
- 今の状況で最小に進めるならどちらか
- 今回は入れないが将来のために受け皿だけ作るべきもの

ただし、必要性の低い質問はしない。
質問なしで進める場合は、**採用した仮定を1行で明示**してから実装する。

---

## 16. 実装前に「今回の要求仕様」を自分で短く書く

毎回ユーザーが詳細な指示書を書かなくても進められるように、作業開始前に自分で今回分の要求仕様を作る。

フォーマットは簡潔でよい。最低でも以下を含める。

- タイトル
- 目的
- 背景
- スコープ
- 設計方針
- 最小仕様
- 非対象
- 受け入れ条件
- 地雷（やってはいけないこと）

この要求仕様は、直近の指示と既存のinstructionsに整合していなければならない。

---

## 17. まず「どの層で解くか」を決める

問題が起きたとき、すぐコードを足さない。
まずそれがどの層の責務かを決める。

- 入力解釈で解くべきか
- 正本モデルで解くべきか
- 派生形状で解くべきか
- 表示だけで解くべきか

誤った層で解くと、仕様が増殖し、後で破綻する。
特に以下を守る。

- 見た目の改善を正本に混ぜない
- 入力都合を正本に持ち込まない
- 表示都合で生成規則を汚さない

---

## 18. 問題の再発を防ぐには「一般化」ではなく「原因の型」を潰す

同じ種類の問題が再発した場合、局所修正を繰り返さない。
その問題の“型”を特定して潰す。

例:
- 更新漏れ
- 入口の複数化
- 正本/派生の混在
- 軸定義の不一致
- 旧経路への逆流
- 自由度の増えすぎ
- テンプレ規則をコードで守れていない

修正提案は、現象そのものではなく「原因の型」に対して行うこと。

---

## 19. 仕様が増殖しそうなときは「2段階導入」にする

複雑な要求（分岐、剛性、交差点、可変規格、空中ノード等）は、一度に完成形を入れない。

必ず次のどちらかで進める。

- 段階1: 器だけ作る（型、フラグ、ポリシー、受け皿）
- 段階2: 実際の自動生成/振る舞いを入れる

「いま必要な挙動」と「将来必要になる受け皿」を分ける。
受け皿だけ先に入れる場合は、既存挙動を変えないこと。

---

## 20. 見た目最優先の領域では「厳密物理」より「安定した近似」を選ぶ

線、曲線、たるみ、交差点、分岐など、見た目が主目的の領域では、最初から厳密物理解を目指さない。

優先順位:
1. 破綻しない
2. 再現性がある
3. 編集しても揺れない
4. それっぽく見える
5. 必要なら後で物理寄りに近づける

このとき、詳細形状のポリシーは正本から分離する。
正本にベジェ制御点や物理中間量を持ち込まない。

---

## 21. テンプレ・規格・ポリシーを優先し、例外を埋め込まない

ユーザーが「高圧は固定3本」「これは空中分岐しない」など規格を意図している場合、それはテンプレ/ポリシーで表現する。

- 固定規格はテンプレで固定
- 可変はテンプレが許可した範囲だけ
- UI制限だけに頼らない
- 例外条件をif文で散らさない

規格はコードで破れない形にする。

---

## 22. 実装のたびに「今回のズレやすい点」を3つ予測する

作業前に、今回ズレやすい点を3つ挙げる。

例:
- 旧APIをまた使ってしまう
- 入力と正本を混ぜる
- 派生データを正本に保存する
- 左右の定義が複数になる
- ローカル判断で隣接整合を壊す
- 一見便利なfallbackで仕様が増殖する

この予測を書いてから実装する。
作業後に、その予測が実際に起きなかったかを自己監査で確認する。

---

## 23. 実装完了前に「この変更は何を将来やりやすくし、何をまだやらないか」を明示する

ユーザーは将来拡張を意識していることが多い。
そのため、毎回の変更で以下を整理する。

- 今回やったことで将来やりやすくなったこと
- 今回はまだやっていないこと
- 今回あえて受け皿だけにしたこと
- 今回無理に入れると危険なこと

これを書かずに終えると、ユーザーは毎回同じ整理をし直す必要が出る。

---

## 24. 直近の指示が抽象的でも、最終的には「テストできる言葉」に変換する

以下のような曖昧語を、そのまま実装要件に使わない。

- それっぽい
- 自然
- ねじれない
- 柔軟
- 汎用的
- 破綻しない
- 空中分岐っぽい
- 曲げ剛性がある感じ

必ず、最終的に観測可能な条件へ変換する。

例:
- 「ねじれない」→ 隣接区間で左右順が反転しない
- 「破綻しない」→ 更新後に依存キャッシュが再生成される
- 「汎用的」→ viewerのPickResultだけに依存し、engine APIをcoreに入れない
- 「自然」→ 可能ならG2、無理ならG1に劣化

---

## 25. ユーザーの負担を減らすことも要件

ユーザーが毎回詳細仕様を書かないと進められない状態は失敗とみなす。
そのため、あなたは毎回以下を目指す。

- 高位の意図から、自分で要求仕様に落とす
- その要求仕様をユーザーに確認しやすい形で短く提示する
- ユーザーが毎回“詳細化担当”にならなくて済むようにする

ただし、勝手に話を広げない。
抽象から具体へ落とすが、スコープは広げない。
