# AGENTS.md

## 目的
このリポジトリでは、見た目の自然さと内部整合の両方を重視する。
内部整合が取れていても、人間が見て不自然なら未完了とする。

## 基本原則
- 正本直書き禁止。公開 API または既存の正規更新経路を使う。
- Manual 保持優先。全体再生成を既定にしない。
- Input / 正本 / 派生を混ぜない。
- topology distinct と visual distinct は別に扱う。
- docs・code・tests・viewer 挙動がズレる可能性を前提に、まず観測事実を優先する。
- 不明な点は不明のまま残し、想像で補完しない。

## 設計方針
- 文章ルールを増やすより、型・API・validator で守る。
- 不正状態を型として作れないようにする。
- 生の更新口を増やさない。中間構造や可変コンテナへの直接追加を避ける。
- fallback は必要最小限にし、複数段で同じ意味を再解釈しない。
- 同じ意味の決定を複数層で別々に持たない。決定結果は可能な限り一箇所で確定し、下流はそれを使う。
- 旧経路や互換用分岐は「使っていないなら残す」のではなく、削除候補として扱う。

## 実装時の判断基準
- まず、直近の指示から以下を整理する。
  - 決定事項
  - 非対象
  - 受け入れ条件
- 局所修正で済むなら局所で直す。
- ただし、同じ意味を別段で再判定しているなら構造改善を優先する。
- bool や番兵値を安易に増やさない。
- 新しい例外分岐を足す前に、既存の責務境界が正しいかを確認する。

## 検証
- メトリクスを通しただけで完了扱いにしない。
- 可能なら数値観測を残す。
- 可能なら validator / test で不変条件を固定する。
- refresh / recalc 後に意味が痩せていないかを確認する。
- viewer で違和感がある場合、core 幾何の問題か viewer 表示の問題かを切り分ける。

## 役割
### sirabe
- 調査専任。
- docs・code・tests・viewer 挙動を読み、観測事実、依存、影響範囲、検証観点を整理する。
- 実装方針の断定はしない。
- 仕様と実装とテストを混ぜて書かない。

### nakami
- core 側担当。
- relation、判定、solver、state、validator、tests を扱う。
- データ構造、型、安全な API、責務整理を重視する。
- 「決定結果を持つ」だけでなく、「それを通らないと作れない構造」に寄せる。

### gamen
- viewer 側担当。
- DrawPath、pick、hover、selection、overlay、debug 表示、最終見え方を扱う。
- core の決定を viewer がどう消費しているかを確認する。
- UI を増やして問題を隠さず、まず repro と切り分けを優先する。

## 複数 agent で作業するときのルール
- まず sirabe が層と影響範囲を切る。
- 同じファイル、同じ型、同じ責務境界を同時に触らない。
- 着手前に以下を明示する。
  - 今回触る層
  - 今回触らない層
  - 競合しうるファイル
  - 先に固定する境界
- core と viewer が同じ意味を別々に持ち始めたら止める。
- 後から合流するときは、どの値を正本にするかを先に決める。

## 出力時に必ず書くこと
- 何を観測したか
- 何を変えたか
- どの責務に寄せたか
- どの不変条件を守るようにしたか
- 何が未完か
- 次に見るべき点

## ここに書かないもの
- 直近の個別バグ
- 今回だけの仕様
- 作業中の閾値
- 過去ログの長い要約
- 一時的な検索監視対象
これらは都度の指示文に書く。
