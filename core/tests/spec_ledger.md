# Core Spec Ledger (Phase4.8)

## Scope and policy
- 観測根拠: 公開API戻り値、`view()`、`connection_index()`、`relation_index()`、`find_*cache()`、`port_resolution_debug_records()`、`last_path_direction_debug()`、`last_generation_edge_orientations()`、`SavedBackboneResult()`、`Validate()` のみ。`last_generation_backbone` 系は midair 入力まわりの一時 snapshot 観測としてのみ扱う。
- 期待値粒度: `Exact` は決定論のみ、`Invariant` は不変条件のみ。
- モック方針: ドメインロジックのモック禁止（本スイートはモック未使用）。
- 時間/並行: 実時間待ち・非決定並行を使わない。
- 分類ルール: `分類=Symptom` は最終症状やユーザ価値を直接固定する行、`分類=Authority` は正本・ownership・decision origin を固定する行。
- Stage ルール: `Init / Pick / Decision / LanePrep / SupportLayout / Curve/Render / Commit / Refresh / Regenerate / Validate / Generate/Edit / General` の coarse 区分で、主に契約が切り替わる段を付ける。
- 永続化 Boundary: `CoreStateAuthoritativeStorage` へ field を追加する変更は、serializer・deserializer・roundtrip test の更新を伴う。

| Case ID | 分類 | Stage | 目的 | 前提 | 入力 | 期待結果 | 観測点 | 壊れた時に守りたいユーザ価値 |
|---|---|---|---|---|---|---|---|---|
| C750 | Invariant | Commit | authoritative save は version 付きで決定的かつ編集を反映する | 66 pole級 route を生成済み | SerializeAuthoritative x2 → 別route生成 → SerializeAuthoritative | Invariant: header は `wire_state_v1`、同一stateはbyte一致、編集後はbyte差分 | serialized text | 保存結果の非決定性と編集の取りこぼしを防ぐ |
| C751 | Invariant | Refresh | authoritative load は派生出力をbit一致で再導出する | geometry/visual設定変更済み3点route | save→新CoreStateへload | Invariant: layout、curve全sample、bounds、visual parts、run idがbit一致 | public derived views | cacheを保存せず同じ表示と形状を復元する |
| C752 | Symptom | Commit | load後の再saveは元byteと一致する | 生成済みstateのsave | load→save | Exact: byte一致 | serialized text | 保存と再読込の反復でproject fileを変質させない |
| C753 | Symptom | Generate/Edit | load後もID衝突なく編集を継続できる | route生成済みsave | load→既存終端から延長→template編集 | Invariant: 生成と編集が成功し全IDが一意 | next_id / public stores | 読込後の編集で既存objectを上書きしない |
| C754 | Boundary | Commit | 不正loadは本stateを変更しない | 生成済みstateと正常save | version不一致、未知key、途中切断 | Boundary: 明示error、stateの再save byte不変 | EditResult.error / serialized text | 壊れたfileの部分適用を防ぐ |
| C762 | Boundary | model adapter | ModelDescriptorからassembly partへstable socketを値コピーする | 2 named socketsとdescriptor versionを持つ測定値 | build_model_assembly_part | part id/model key/version/transform/socket値が一致し、空keyとsocket名重複はconflict | ModelAssemblyPartBuildResult | transient descriptorへのpointer保持やscene geometryからのsocket再推測を防ぐ |
| C763 | Boundary | model assembly / Persistence | assembly登録はvalidation後だけ反映し、wire socketとendpoint mount socketを含めsave/load対象になる | 両socket付きassembly、未解決socket参照、旧mount fieldなしarchive | RegisterModelAssemblyTemplate→SerializeAuthoritative→DeserializeAuthoritative | 正常assemblyはfield一致、未解決socketと重複idは拒否されstate byte不変。旧archiveはmountなしとして読める | ModelAssemblyTemplate / serialized text | adapterが正本mapを直書きする、不完全assemblyを部分反映する、またはmount追加で旧saveを読めなくする回帰を防ぐ |
| C01 | Symptom | Init | ID生成の単調増加 | 新規IdGenerator | next/peek/reset | Exact: 連番・重複なし | 戻り値 | 永続ID衝突防止 |
| C02 | Symptom | Init | ObjectStore整合 | 空Store | insert/find/remove/upsert | Exact: 件数・参照整合 | 公開API | 参照崩壊防止 |
| C03 | Symptom | Generate/Edit | Span追加初期Dirty | 有効Port2点 | AddSpan | Exact: runtime生成+dirty | runtime | 再計算漏れ防止 |
| C04 | Symptom | Generate/Edit | MovePole局所更新 | 独立Span2本 | MovePole | Invariant: 関連Spanのdata versionだけ更新 | runtime version | 無関係再計算抑制 |
| C06 | Authority | Generate/Edit | PoleType適用 | PoleTypeあり | ApplyPoleType | Invariant: owner整合でPort生成 | PoleDetail | テンプレ適用維持 |
| C07 | Symptom | LanePrep | PoleType差分 | PoleType2種 | 各Type適用 | Invariant: port placement hint構成差 | template_side/template_layer | 型差分維持 |
| C19 | Symptom | Generate/Edit | interval Pole生成 | PoleTypeあり | GenerateFromBackboneSpec(interval) | Invariant: 本数/Type/saved graph node | poles/backbone | interval 自動配置信頼性 |
| C20 | Symptom | Generate/Edit | 短polyline拒否 | PoleTypeあり | GenerateFromBackboneSpec(点1) | Exact: fail+状態不変 | error/count | 入力ミス耐性 |
| C22 | Symptom | Generate/Edit | 存在しないPort拒否 | 空状態 | AddSpan(無効ID) | Exact: fail+状態不変 | error/count | 参照不正耐性 |
| C28 | Symptom | Generate/Edit | through連続性 | 直線入力 | GenerateFromBackboneSpec | Invariant: 中間Pole同Port再利用 | span端点Port | 幹線連続維持 |
| C29 | Symptom | Init | 表示ID採番 | 新規CoreState | Pole/Port/Span追加 | Exact: prefix別連番 | display_id | UI追跡性 |
| C61 | Invariant | Generate/Edit | 鋭角は倍率拡幅ではなく径間別rowで実幅を保つ | 45度/120度の同一2-lane template比較 | GenerateFromBackboneSpec | Invariant: 鋭角は2 open row、緩角は1 pair row、port side scaleは1（カテゴリ非依存） | SavedBackboneRowKey / Port.side_scale_applied | 鋭角でゼロへ近づく投影幅を大倍率で追う回帰防止 |
| C101 | Symptom | Generate/Edit | HV空中分岐規格フラグ固定 | HV template | GenerateFromBackboneSpec(HV_3PH, legacy branch mode値を注入) | Exact: fail（unsupported mode）かつ `allow_midair_branch=false` | error / bundle_templates | 高圧規格逸脱の混入防止 |
| C105 | Authority | Pick | Segment中間でMidair生成 | Segmentクリック（端点から十分離れる） | ResolveBranchPick(PickResult, snap_radius) | Invariant: Midair SupportNodeが生成される | ResolveBranchPick結果 / generation backbone snapshot nodes (`last_generation_backbone.nodes`) | 空中分岐入力を安定して扱える |
| C106 | Authority | Pick | Pick経由HV空中分岐禁止 | Segment中間クリック + HV template | ResolveBranchPick(PickResult, HV) | Exact: fail（midair branch禁止） | error | 高圧規格逸脱の混入防止 |
| C107 | Authority | Pick | Midair Dry-run非破壊 | Segment中間クリック + create_midair_node=false | ResolveBranchPick(PickResult, LV) | Invariant: Midair解決しても generation backbone snapshot の `nodes` が増えない | ResolveBranchPick結果 / generation backbone snapshot nodes (`last_generation_backbone.nodes`) | ホバー評価で状態が汚れる回帰防止 |
| C64 | Symptom | Generate/Edit | Guide頂点の強制Manual解除 | 3頂点Guide | GenerateFromBackboneSpec(既定設定) | Exact: 中間頂点/端点PoleともにAuto（既定） | pole placement_mode | DrawPath点=強制Pinの回避 |
| C65 | Symptom | Generate/Edit | pin_verticesオプション | 3頂点Guide | GenerateFromBackboneSpec(pin_vertices=true) | Exact: 中間頂点PoleもManual | pole placement_mode | ピン留め挙動の明示制御 |
| C66 | Symptom | General | Pole Pin/Unpin切替 | 既存Pole | SetPolePlacementMode(Auto→Manual→Auto) | Exact: mode遷移とuser_edited整合 | pole fields | ユーザー明示ピン留め運用 |
| C71 | Symptom | Generate/Edit | MovePoleでAuto Port再投影とManual保護 | 既存Pole + Manual Portあり | MovePole | Invariant: Auto Portだけが再投影され、Manual Portは維持される | port位置 / runtime dirty | Pole移動で手修正Portが壊れない |
| C44 | Authority | Validate | Bundle参照API整合 | Span1本+Bundle作成 | AddBundle + AddSpan + GetSpansByBundle | Invariant: Spanがbundle参照を保持し検索整合 | span fields/query API/Validate | 複数本配線の正本一貫性維持 |
| C45 | Symptom | Generate/Edit | 不正Bundle参照拒否 | Span1本 | AddSpan(bundle_id不正) | Exact: failし状態保全, 診断文言あり | error/span fields | 不正参照でデータ破壊しない |
| C90 | Symptom | Generate/Edit | Backbone bundles必須 | BackboneSpecでbundles空+legacy値のみ設定 | GenerateFromBackboneSpec | Exact: bundle 必須エラー | error | legacy経路の逆流防止 |
| C74 | Symptom | Generate/Edit | 非HVテンプレ固定1本 | BackboneSpec + LV既定テンプレ | GenerateFromBackboneSpec(bundle=count未指定) | Invariant: 区間ごとに1本生成 | generated span数/bundle count | 規格固定で入力削減 |
| C75 | Symptom | Generate/Edit | 非HV固定テンプレcount上書き拒否 | BackboneSpec + LV既定テンプレ | GenerateFromBackboneSpec(bundle=count指定) | Exact: fail（上書き不可） | error | 固定規格の強制 |
| C77 | Symptom | Generate/Edit | 複数テンプレ同時生成 | BackboneSpec + LV/COMM複合指定 | GenerateFromBackboneSpec(bundles複数) | Invariant: bundleが複数生成され、Span数が合算本数に一致 | result.bundle_ids/generated spans | 複数束同時入力の成立 |
| C80 | Symptom | General | center hintのPole非重なり | center hintを持つPoleType | PoleType適用でPort生成 | Invariant: center候補由来PortはPole中心線から半径+クリアランス分だけ離れる | port local/world位置 | center PortがPoleに埋まらない |
| C50 | Symptom | General | Port初期モード | 新規Port追加 | AddPort | Exact: position_mode=Auto | port fields | 新規Port生成規則の固定 |
| C52 | Symptom | Regenerate | Manual保護 | 手修正Portあり | SetPoleFlip180 | Invariant: Manual Port位置が維持される | port position/mode | 軽微再生成で手修正消失防止 |
| C53 | Symptom | Generate/Edit | BackboneSpec境界手動点安定 | BackboneSpec初回生成済 | BackboneSpec延長して再GenerateFromBackboneSpec | Invariant: 既存Manual境界Poleの位置/Mode不変 | pole position/mode | 軽微変更で手直し消失防止 |
| C55 | Symptom | General | Backbone経路 | Bundle付きSpan生成済 | SavedBackboneEdges/FindSavedBackboneRoute | Invariant: bundle付きedge構築と経路取得 | backbone edge/route | ルート計算基盤維持 |
| C58 | Symptom | Generate/Edit | Reverse対称性 | 同一Guide | Forward/ReverseでGenerateFromBackboneSpec | Invariant: 生成Pole位置集合が一致（順序非依存） | generated pole positions set | 方向モードで幾何が破綻しない |
| C59 | Symptom | Generate/Edit | Avoid制約尊重 | PoleTypeあり | avoid_points/avoid_radius指定GenerateFromBackboneSpec | Invariant: 生成Poleが禁止半径内に入らない | pole positions vs avoid radius | 回避制約の信頼性 |
| C117 | Authority | Pick | Path input では midair policy を入力段階で強制しない | midair pick + HV template + enforce=false | ResolveBranchPick(PickResult, HV) | Invariant: template branch policy を強制せず Midair 解決できる | ResolveBranchPick結果 / generation backbone snapshot nodes (`last_generation_backbone.nodes`) | DrawPath の入力段階で不要に操作を止めない |
| C121 | Authority | General | Template責務の分離 | 型定義 | compile-time traits | Exact: allow_midair_branch は BundleTemplate 側のみで、CableTemplate と entity は bezier入力や arc-length table を持たない | type traits | 正本とテンプレ責務の混線防止 |
| C122 | Symptom | Generate/Edit | CableTemplate編集で Pole tilt を上書きしない | Pole tilt 設定済み + CableTemplate編集 | SetPoleTilt(max) + UpdateCableTemplate | Invariant: pole の tilt magnitude と実際の rotation X/Y は不変 | pole tilt_magnitude_deg / pole world_transform | Pole傾きをテンプレ変更が壊さない |
| C123 | Invariant | Generate/Edit | BundleTemplate の topology 変更は backbone scope を regenerate する | LV backbone route + `default_layer`変更 | UpdateBundleTemplate | Invariant: update成功後のtemplateは新layerになり、binding不一致のspan/portはChangeSetで退役+再生成される | BundleTemplate / Span / Port / ChangeSet | topology変更をrejectまたはvisual-onlyで済ませる回帰防止 |
| C124 | Symptom | Regenerate | BundleTemplate の visual-only 変更は dirty のみに留める | 既存bundle + cable_template_id変更 | UpdateBundleTemplate | Invariant: 依存 span は dirty になるが regeneration_required は立たない | span runtime / template_dependency_state | 見た目変更を topology再生成に混ぜない |
| C125 | Symptom | General | ApplyPoleTilt は選択された Pole 実体だけを更新する | 2本のPoleの片方だけ選択 | ApplyPoleTilt(selection, max) | Invariant: 指定Poleだけ tilt magnitude と rotation X/Y が変わる | pole tilt_magnitude_deg / pole world_transform | Pole tilt を実体値として局所更新できる |
| C202 | Symptom | General | Pole tilt は incident span 方向へ寄る | 1本の接続 span を持つ Pole | SetPoleTilt(max) | Invariant: 傾き方向は完全ランダムではなく incident span の水平合成方向へバイアスする | pole top delta / remote port direction | Pole tilt が接続方向と無関係に倒れて見える回帰防止 |
| C203 | Symptom | General | Pole tilt 量は pull imbalance に従う | 直線貫通Pole と 角度付きPole | SetPoleTilt(max) | Invariant: 水平 pull が釣り合う Pole より、pull が偏る Pole のほうが tilt magnitude が大きい | pole tilt_magnitude_deg | 懸垂の効きが弱く、直線と角度付きで同じ傾き量になる回帰防止 |
| C126 | Symptom | General | WorldUp と lateral 軸の整合 | 代表的な forward vector | WorldUp + ComputeLateralAxis | Invariant: lateral が forward/up と直交し正規化される | coord utils | 軸依存の手書き計算を1定義に寄せる |
| C127 | Symptom | General | PoleFrame の local/world roundtrip | tilt付き transform + local point | BuildPoleFrame + LocalPointToWorld + WorldPointToLocal | Invariant: tilt 下でも local/world roundtrip が安定する | pole frame math | Pole傾き適用の回転順序ずれ防止 |
| C128 | Symptom | Curve/Render | uベース曲線APIの端点拘束 | 端点位置と端点接線が既知 | BuildDetailCurve + EvaluatePosition/EvaluateTangent | Invariant: u=0/1 で端点位置を満たし、端点接線が意図方向へ揃う | DetailCurve | 曲線生成を u ベースに固定する |
| C129 | Symptom | Curve/Render | sベース配置APIと sag 合成 | 端点固定 + sag 付き曲線 | BuildDetailCurve + PositionAtLength | Invariant: sag 後も端点位置は不変で、長さ基準の中点配置が使える | DetailCurve.arc_length_table | 正確な配置を s ベースで扱える |
| C141 | Symptom | Curve/Render | 懸垂寄り support slope | 端点同高 + sag 付き曲線 | BuildDetailCurve | Invariant: sag 付き曲線は支点で完全水平に寝ず、両端で上下向きの傾きが出る | DetailCurve.EvaluateTangent | quartic 的な平坦 sag に戻る回帰防止 |
| C142 | Symptom | Curve/Render | 近直線での横揺れ抑止強化 | 同高ほぼ直線 + 軽い逆向き横tangent | BuildDetailCurve | Invariant: 近直線 span は lateral tangent 差があっても平面内でほぼ真っ直ぐに保たれる | DetailCurve.sample_points | 微小な横成分で左右にぐにゃる回帰防止 |
| C143 | Symptom | Curve/Render | SmoothPass の横揺れ抑止 | 同方向へ緩く曲がる長尺 span | BuildDetailCurve | Invariant: SmoothPass は G2 継続を保ちつつ top-view の lateral bend を抑える | DetailCurve.quality / sample_points | 継続カーブで不要な左右揺れを残す回帰防止 |
| C130 | Symptom | Curve/Render | OffsetEndpoint 端点 | 支点と派生 endpoint offset が既知 | BuildDetailCurve(OffsetEndpoint) | Invariant: 曲線端点は支点そのものではなく派生 offset 位置になる | DetailCurve | 支点近傍で急に折れない離脱表現の土台 |
| C131 | Symptom | Curve/Render | 鋭角/競合接線での品質劣化安全策 | 反対向きに近い接線ヒント | BuildDetailCurve | Invariant: tangent fallback が働き、過度な膨張や逆行を抑える | DetailCurve.quality / sample_points | 高度最適化なしで見た目破綻を抑える |
| C148 | Symptom | Curve/Render | SharpCorner の compact 化 | 鋭角 corner-pass + 強い横tangent | BuildDetailCurve | Invariant: SharpCorner は handle を縮めて G1 に落ち、横ピークを抑える | DetailCurve.quality / sample_points | 鋭角部まで SmoothPass 的に膨らむ回帰防止 |
| C144 | Symptom | Curve/Render | 長尺継続 span の G2優先採用 | 長い pass-through span + 素直な端点接線 | BuildDetailCurve(PreferG2) | Invariant: continuity は G2 になり、smooth pass-through 理由が debug で追える | DetailCurve.quality | G2優先方針が単なる係数でなく判定として残ることを固定 |
| C145 | Symptom | Curve/Render | 短スパンの G1劣化 | 短い span + PreferG2 | BuildDetailCurve | Invariant: 短スパンでは無理に G2 を維持せず G1 と ShortSpan 理由へ落ちる | DetailCurve.quality | 短スパンで過拘束 cubic を押し込む回帰防止 |
| C146 | Symptom | Curve/Render | branch pass の G1優先と端点拘束保持 | branch pass + offset endpoint | BuildDetailCurve | Invariant: branch pass は G1 を採用しつつ offset endpoint と端点接線を保つ | DetailCurve.quality / EvaluatePosition / EvaluateTangent | branch で G2 に固執して support 離脱が崩れる回帰防止 |
| C147 | Symptom | Curve/Render | PreferG1 は明示選択 | 長尺 span + PreferG1 + sag | BuildDetailCurve | Invariant: PreferG1 は failed G2 ではなく explicit G1 として扱われ、sag と arc-length はそのまま動く | DetailCurve.quality / arc_length_table | continuity 設定と実挙動の意味ずれ防止 |
| C149 | Symptom | Curve/Render | ViaAttachment の endpoint 優先 | offset endpoint を使う attachment 離脱 | BuildDetailCurve(OffsetEndpoint) | Invariant: ViaAttachment は attachment endpoint を端点に使い、smooth pass を強制せず endpoint-priority G1 へ落ちる | DetailCurve.quality / EvaluatePosition | attachment 接続を支点中心通過へ戻す回帰防止 |
| C151 | Symptom | General | worldspace variation の連続性 | 近接/遠方の world position | EvaluateHierarchicalVariation | Invariant: 近い位置の bias 差は小さく、遠い位置では差が広がる | HierarchicalVariationSample.world_bias | ID乱数で worldspace を離散化する回帰防止 |
| C171 | Symptom | PublicAPI | public header smoke surface | 公開ヘッダのみを含む最小 consumer | include public headers + instantiate public types | Invariant: 公開ヘッダが self-sufficient で、concept-level view を内部 storage に触れずに使える | EntityRef / SpanLayoutView / SpanLayoutState / CoreView inspection surface | 公開ヘッダの依存漏れや内部 storage 断面への依存が利用者ビルドへ漏れる回帰防止 |
| C181 | Symptom | Decision | GetPoleDetail は対象 pole の owned endpoint だけを返す | pole A/B と owned port/anchor | AddPort / AddAnchor → GetPoleDetail | Invariant: pole A detail は A 所有の port/anchor だけを含み、pole B の endpoint を混ぜない | CoreState::GetPoleDetail | owned endpoint の公開観測面が他 pole を混ぜたり relation index 直参照前提に戻る回帰防止 |
| C182 | Symptom | Generate/Edit | MovePole は対象 pole の owned endpoint だけ更新する | pole A/B と owned port/anchor、Aだけ移動 | MovePole | Invariant: pole A の owned endpoint だけ位置と change_set が更新され、pole B 側は不変のまま | CoreState::MovePole / ChangeSet | pole 移動が他 pole の owned endpoint まで巻き込む回帰防止 |
| C200 | Authority | Generate/Edit | bundle の branch down offset policy 変更は topology regenerate | HV backbone route | UpdateBundleTemplate | Invariant: `enable_branch_down_offset`変更は成功し、新template値と対象spanのChangeSet更新が一致する | CoreState::UpdateBundleTemplate / BundleTemplate / ChangeSet | branch-down policy 変更が no-opまたはvisual-only扱いになる回帰防止 |
| C277 | Symptom | Curve/Render | 高低差の大きい smooth span は composite curve へ切り替える | 高位置 support から低位置 support への長尺 pass-through span | BuildDetailCurve | Invariant: upstream で composite hint が立った smooth pass は端点拘束と s ベース評価を保ったまま internal segment 数が 1 より大きくなり、単一 cubic の急なつなぎへ戻らない | CurveConstraint.profile_hint / DetailCurve.segment_count / EvaluatePosition / arc_length_table | 高低差 span が単一 cubic へ戻り、support 近傍で不自然に直線化・急変する回帰防止 |
| C161 | Symptom | Curve/Render | 長い branch span は横回り込みを抑える | 長尺 branch + 強い横向き support tangent | BuildDetailCurve(BranchPass) | Invariant: branch は chord 優先で support departure を局所化し、大きな sideways runout を抑える | DetailCurve.quality / sample_points | branch support の横向きが span 全体を左右へ引っ張る回帰防止 |
| C162 | Symptom | Curve/Render | branch の局所 departure は短スパンほど強い | 短尺/長尺 branch を同一 tangent で比較 | BuildDetailCurve(BranchPass) | Invariant: 短スパンの方が support weight と lateral limit が高く、長スパンでは chord 優先が強まる | DetailCurve.quality | branch の横成分抑制が span 長に応じて変わらない回帰防止 |
| C163 | Symptom | Curve/Render | main の sag は branch より強く読める | 同一 chord/hint の main と branch | BuildDetailCurve | Invariant: endpoint 拘束を保ったまま main の中央下がりが branch より強い | DetailCurve.sag_amplitude / EvaluatePosition | branch まで main と同じ sag 読みになって垂れすぎる回帰防止 |
| C287 | Symptom | Init | CommunicationPole は support/HV/LV/optical/communication/drop の高さ順を保つ | デフォルト pole type 一覧を観測 | CoreState 初期化 | Invariant: CommunicationPole の top support band は HV より高く、HV は LV より高く、LV は optical より高く、optical は communication より高く、communication は drop より高い | PoleTypeDefinition.port_bands | CommunicationPole の default placement 高さ順が崩れて Pole Placement のカテゴリ意味が逆転する回帰防止 |
| C289 | Symptom | Generate/Edit | pole type 高さ更新は既存 pole 本体高さと owned auto ports へ再適用される | CommunicationPole を適用した pole 1 本の optical bands と pole 高さを更新 | ApplyPoleType→UpdatePoleTypeDefinition | Invariant: PoleTypeDefinition の default height と port-band 高さ変更は既存 pole 本体高さと auto-owned optical ports に反映され、template と実配置が乖離しない | UpdatePoleTypeDefinition / Pole.height_m / PoleDetailInfo.owned_ports / Port.world_position.z | pole type 編集後も既存 pole の本体高さや port 高さが stale のまま残る回帰防止 |
| C296 | Authority | Init | default pole template の HV category は単一高さを使う | デフォルト pole type 一覧を観測 | CoreState 初期化 | Invariant: DistributionPole / CommunicationPole の enabled HV bands は同一 `height_center_m` を共有し、category-level pole placement UI が平均値ではなく正本高さを読む | PoleTypeDefinition.port_bands | HV band ごとに高さが微妙にずれていて category UI の初期値だけ高く見える回帰防止 |
| C297 | Invariant | Generate/Edit | related pole type の post-edit は backbone を regenerate して適用する | backbone line に optical bundle を追加 | ApplyBundleRelatedPoleTypeToExistingPoles | Invariant: related pole type 適用は成功し、対象 pole の type/height が related template へ変わり validator が通る | SavedBackboneGraph / BundleTemplate.related_pole_type_id / Pole.pole_type_id | 旧直接適用や拒否で SavedGraph と実体を不整合にする回帰防止 |
| C304 | Authority | General | style context resolver は semantic key ごとに deterministic で route-level 傾向を共有する | 同一 `ContextProfile` / `StyleRouteKey` に対して sibling object key を 2 つ解決 | ResolveStyleContext | Invariant: district/route scope は同じ semantic route で共有され、同一 object key では完全再現し、object key だけ変えると object scope だけが変わる | ResolveStyleContext / ResolvedStyleContext.scope / route / object | realism resolver が transient id や呼び出し順に依存して不安定になり、route 相関や再現性を失う回帰防止 |
| C309 | Symptom | Init | CommunicationPole の communication / optical 既定 band は中央 side を保つ | デフォルト pole type 一覧を観測 | CoreState 初期化 | Invariant: CommunicationPole の enabled communication / optical trunk bands は `SlotSide::kCenter` を保ち、左右分離を既定にしない | PoleTypeDefinition.port_bands | CommunicationPole の既定 band が再び left/right に戻り、光ケーブルや通信ケーブルが pole 配置で左右へ逃げる回帰防止 |
| C310 | Symptom | Init | default template は optical-with-support の分岐 type を登録しない | デフォルト bundle template 一覧を観測 | CoreState 初期化 | Invariant: default bundle template には `kOpticalWithSupport` が現れず、optical は 1 種類の bundle として扱う | CoreView.bundle_templates | optical だけ支持線有無で bundle type が分かれて viewer 上の type が増える回帰防止 |
| C313 | Symptom | Init | CommunicationPole の default Pole Placement 値は viewer 既定値と一致する | デフォルト pole type 一覧を観測 | CoreState 初期化 | Invariant: CommunicationPole の category-level default placement は HV=9.20/0.00/0.60, LV=8.03/0.00/0.40, COMM=6.25/0.22/0.00, OPT=6.60/0.04/0.00, DROP=4.20/0.00/0.00 を返す | PoleTypeDefinition.port_bands | default template と Pole Placement UI の初期値がずれて、起動直後からユーザ想定と違う配置になる回帰防止 |
| C357 | Invariant | StateService | cable decision 更新は backbone 出力を regenerate する | backbone LV span + continuity変更 | UpdateCableTemplate | success、template と curve requested policy が編集後 policy になる | CableTemplate / CurveCacheEntry | decision変更を stale 成功や未再導出にしない |
| C368 | Invariant | backbone | backbone milestone 1 は単純 line slice を生成する | 2点 path + single bundle | GenerateFromBackboneSpec | Invariant: pole 2本、bundle 1つ、template count 分の span が生成される | GenerateBundleFromPathResult / CoreView stores | v2 が入口で最小縦スライスを通せない回帰防止 |
| C369 | Invariant | backbone | backbone は生成 span ごとに SpanLayoutRules を保存する | C368 と同じ | GenerateFromBackboneSpec→span_layout_rules | Invariant: generated span は `SpanLayoutRulesView.has_rule=true` | CoreState::span_layout_rules | v2 が旧 authority/seed ではなく保存済み rules を正本にする境界確認 |
| C370 | Boundary | backbone | backbone は旧 contract / recalc 内部に依存しない | source scan | wire_core_tests | Boundary: `core/src/generation/backbone` に support-layout contract / recalc / materialization 系の禁止語がない | source text | v2 が旧 support-layout/recalc wrapper へ戻る回帰防止 |
| C371 | Boundary | backbone | backbone は未対応入力を fallback せず拒否する | empty bundle / missing saved building node spec | GenerateFromBackboneSpec | Boundary: error に unsupported を含み、v1 fallback しない | EditResult.error | 未対応機能を v1 fallback で埋めて境界が崩れる回帰防止 |
| C372 | Boundary | backbone | backbone は rules 保存で追加入力を要求しない | 2点 path + single bundle | GenerateFromBackboneSpec→span_layout_rules/span_layout_state | Boundary: rules は存在し、layout state は input_required=false | SpanLayoutRulesView / SpanLayoutState | backbone が rules-only 正本から旧 seed/authority contract へ戻る回帰防止 |
| C373 | Boundary | backbone | backbone は recalc なしで rules から layout を保存する | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: Commit 前に rules と layout が存在し、追加入力は不要 | SpanLayoutRulesView / SpanLayoutView / SpanLayoutState | backbone が生成後に dirty/recalc/materialization へ戻らない境界確認 |
| C374 | Invariant | backbone | backbone layout 導出は同一入力で決定的 | fresh state 2つに同じ 2点 path + single bundle | GenerateFromBackboneSpec | Invariant: generated span の start/end support_world と endpoint_world が一致する | SpanLayoutEntry | cache や既存状態で layout 派生が揺れる回帰防止 |
| C375 | Boundary | backbone | backbone は recalc なしで layout から curve を保存する | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: Commit 前に rules/layout/curve が存在し、追加入力は不要 | CurveCacheEntry / SpanLayoutView / SpanLayoutState | backbone が curve 生成で recalc/materialization へ戻らない境界確認 |
| C376 | Invariant | backbone | backbone curve 導出は同一入力で決定的 | fresh state 2つに同じ 2点 path + single bundle | GenerateFromBackboneSpec | Invariant: generated span の curve sample_points と total_length_m が一致する | DetailCurve | cache や既存状態で curve 派生が揺れる回帰防止 |
| C377 | Boundary | backbone | backbone は recalc なしで curve から bounds を保存する | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: Commit 前に rules/layout/curve/bounds が存在し、追加入力は不要 | BoundsCacheEntry / CurveCacheEntry / SpanLayoutState | backbone が bounds 生成で recalc/rebuild へ戻らない境界確認 |
| C378 | Invariant | backbone | backbone bounds 導出は同一入力で決定的 | fresh state 2つに同じ 2点 path + single bundle | GenerateFromBackboneSpec | Invariant: generated span の whole/segment AABB min/max が一致する | BoundsCacheEntry | cache や既存状態で bounds 派生が揺れる回帰防止 |
| C379 | Boundary | backbone | backbone milestone 1 の必須出力を固定する | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: topology/rules/layout/curve/bounds が生成直後に存在する | GenerateBundleFromPathResult / SpanLayoutRulesView / SpanLayoutEntry / CurveCacheEntry / BoundsCacheEntry | milestone 1 の要求仕様が曖昧になり draw cache まで必須扱いされる回帰防止 |
| C380 | Boundary | backbone | backbone milestone 1 は draw cache を保存する | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: generated span に visual/render cache が存在する | SpanVisualCacheEntry / SpanRenderCacheEntry | draw V1 が生成直後の派生出力から抜ける回帰防止 |
| C381 | Boundary | backbone | backbone milestone 1 は recalc contract を持たない | 2点 path + single bundle + source scan | GenerateFromBackboneSpec / wire_core_tests | Boundary: layout state は input_required=false で、backbone source は recalc/Commit/materialization 依存を持たない | SpanLayoutState / source text | recalc 不使用が口約束になり旧 contract が backbone に戻る回帰防止 |
| C382 | Boundary | backbone | backbone geom は pipeline 上 1 層として扱う | source scan | wire_core_tests | Boundary: `struct geom` があり、`make(const curve&)` / `save(curve)` / `save(bounds)` が無い | source text | curve/bounds が pipeline の独立工程へ戻り、milestone 1 の派生層が細切れになる回帰防止 |
| C383 | Boundary | backbone | backbone draw は pipeline 層として扱う | source scan | wire_core_tests | Boundary: `struct draw` / `make(layout, geom)` / `save(draw)` があり、backbone source は cache 内部名を直接読まない | source text | draw が layout/geom 以外の工程へ混ざる回帰防止 |
| C384 | Boundary | backbone | backbone topology 出力は `topo` 1 層として扱う | source scan | wire_core_tests | Boundary: `struct topo` があり、`struct spans` が無く、`graph` は generated poles を持たない | source text | topology 生成結果が graph や span 専用コンテナへ散って責務が読めなくなる回帰防止 |
| C385 | Boundary | backbone | backbone topology emission は poles/bundles/ports/spans に分かれる | source scan | wire_core_tests | Boundary: `emit_poles` / `emit_bundles` / `emit_ports` / `emit_spans` が存在する | source text | `emit()` が topology 作成の大きな便利箱へ戻る回帰防止 |
| C386 | Boundary | backbone | backbone は link/pair/open/row を別概念として持つ | source scan | wire_core_tests | Boundary: `link` / `pair` / `open` / `row` が別型で、row は source を持つ | source text | pair/open/row が別名の便利箱になり、junction 分岐が再発する回帰防止 |
| C387 | Boundary | backbone | backbone は pairs を graph から一箇所で作る | source scan | wire_core_tests | Boundary: `pairs make(const graph&)` があり、build 先頭で一度だけ呼ばれる | source text | pair 確定が emit/rules/layout に分散する回帰防止 |
| C388 | Invariant | backbone | backbone 3点 route は link/pair/open model で生成される | 3点 path + single bundle | GenerateFromBackboneSpec / source scan | Invariant: links/pair/open を作る source があり、pole 3本と 2 segment 分の span が生成される | GenerateBundleFromPathResult / source text | 折れ線対応が segment 直書きに戻り、pair 正本を通らない回帰防止 |
| C389 | Boundary | backbone | backbone row axis は pairs 出力が owner になる | source scan | wire_core_tests | Boundary: emit_ports は row axis を読み、side/norm で軸を作り直さない | source text | port placement が route geometry から axis を再発明する回帰防止 |
| C390 | Boundary | backbone | backbone は不正な pair incident 入力を拒否する | zero-length link path + source scan | GenerateFromBackboneSpec | Boundary: invalid pair input は unsupported で止まり、incident already used 診断経路が存在する | EditResult.error / source text | pair が曖昧でも fallback で補って downstream に流す回帰防止 |
| C391 | Boundary | backbone | backbone は junction kind label を持たない | source scan | wire_core_tests | Boundary: ThroughMain/SideBranch/CrossUnderpass/CornerContinuation 等の role label を backbone に追加しない | source text | T/cross/branch 分岐が別 enum として戻る回帰防止 |
| C392 | Invariant | backbone | backbone は3点 route の全 span に必須出力を保存する | 3点 path + single bundle | GenerateFromBackboneSpec | Invariant: 全 generated span に rules/layout/curve/bounds がある | SpanLayoutRulesView / layout / CurveCacheEntry / BoundsCacheEntry | polyline 対応で rules-only/layout-only/geom 境界が崩れる回帰防止 |
| C393 | Invariant | backbone | backbone 3点 route 出力は決定的 | fresh state 2つに同じ3点 path | GenerateFromBackboneSpec | Invariant: pole positions と curve sample points が一致する | Pole / DetailCurve | pair/row 確定が vector 順や既存状態で揺れる回帰防止 |
| C394 | Boundary | backbone | backbone existing pole node は pole を重複生成しない | 既存 pole を node_specs.node_id に指定した2点 route | GenerateFromBackboneSpec を2回 | Boundary: 2回目は指定始点 pole を作らず、未指定終点 pole だけ生成する | GenerateBundleFromPathResult.generated_pole_ids / CoreView.poles | existing node 対応が新規生成と混ざり、同じ位置に pole を複製する回帰防止 |
| C395 | Invariant | backbone | backbone `is_new` は pair 出力に影響しない | all-new route と existing-start route の同一 geometry | GenerateFromBackboneSpec | Invariant: generated span の curve sample points が一致する | DetailCurve | existing/new の違いが pair/row axis の意味決定に混ざる回帰防止 |
| C396 | Boundary | backbone | existing pole continuation は既存 span を意味決定に再入力せず派生出力を焼き直さない | span 接続済み pole を node_specs.node_id に指定して追加 route を生成 | GenerateFromBackboneSpec を2回 | Boundary: 新規 route の rules/layout/geom は作られ、既存 span の rule/layout/curve/bounds/visual/render/source version は不変で、2回目 ChangeSet の updated/deleted に既存span id が無い | SpanLayoutRulesView / SpanLayoutView / CurveCacheEntry / BoundsCacheEntry / ChangeSet | existing pole 周辺の既存 span/layout を読んで pair や layout を焼き直す回帰防止 |
| C397 | Boundary | backbone | backbone は missing saved midair node spec を拒否する | `SupportKind::kMidair` + missing saved node id | GenerateFromBackboneSpec | Boundary: unsupported で失敗する | EditResult.error | saved graph なしに existing midair reuse を推測する回帰防止 |
| C398 | Boundary | backbone | backbone は存在しない existing pole id を拒否する | missing pole id の node spec | GenerateFromBackboneSpec | Boundary: unsupported で失敗する | EditResult.error | 不正 node id を fallback で新規 pole 化する回帰防止 |
| C399 | Invariant | backbone | backbone existing pole node sequence は決定的 | fresh state 2つで同じ existing pole sequence | GenerateFromBackboneSpec を2回 | Invariant: 新規 pole positions と curve sample points が一致する | Pole / DetailCurve | existing pole node の処理が既存状態順や cache で揺れる回帰防止 |
| C400 | Invariant | backbone | backbone は同じ pairs graph 上で複数 bundle を生成する | 2点 path + LV + Communication | GenerateFromBackboneSpec | Invariant: bundle 2つと各 template count 合計分の spans が生成される | GenerateBundleFromPathResult | multiple bundle が v1 fallback へ戻る回帰防止 |
| C401 | Invariant | backbone | backbone 複数 bundle 3点 route は全 span に必須出力を保存する | 3点 path + LV + Communication | GenerateFromBackboneSpec | Invariant: 全 generated span に rules/layout/curve/bounds がある | SpanLayoutRulesView / layout / CurveCacheEntry / BoundsCacheEntry | multiple bundle 対応で rules/layout/geom 境界が崩れる回帰防止 |
| C402 | Boundary | backbone | backbone bundle spec は pair 出力に影響しない | single bundle と multiple bundle の同一3点 path | GenerateFromBackboneSpec | Boundary: generated pole positions が一致する | Pole.world_transform | bundle spec が pair/row axis の意味決定へ混ざる回帰防止 |
| C403 | Invariant | backbone | backbone existing pole node と multiple bundle は併用できる | existing pole node + LV + Communication | GenerateFromBackboneSpec を2回 | Invariant: existing pole は重複せず、全 bundle の spans が生成される | GenerateBundleFromPathResult / CoreView.poles | existing node と multiple bundle の組み合わせが v1 fallback や pole 複製へ戻る回帰防止 |
| C404 | Boundary | backbone | backbone は empty bundle request を拒否する | `bundles.empty()` | GenerateFromBackboneSpec | Boundary: unsupported で失敗する | EditResult.error | bundle 無し入力を曖昧に成功させる回帰防止 |
| C405 | Boundary | backbone | backbone pairs make は bundle spec を読まない | source scan | wire_core_tests | Boundary: `pairs make(graph)` body に bundle spec / template 参照が無い | source text | bundle 数や種類で pair/axis が変わる回帰防止 |
| C406 | Invariant | backbone | backbone port height は pole band を使う | 2点 path + LV | GenerateFromBackboneSpec | Invariant: generated span endpoint port の Z が selected `PortPlacementBand.height_center_m` と一致する | Port.world_position | 固定高さ 9.32 へ戻る回帰防止 |
| C407 | Invariant | backbone | backbone multiple bundle height は bundle ごとの pole band を使う | 2点 path + LV + Communication | GenerateFromBackboneSpec | Invariant: bundle template ごとに endpoint port Z が対応 band height と一致する | Span / Bundle / Port | 複数 bundle を同一高さへ潰す回帰防止 |
| C408 | Invariant | backbone | backbone existing pole height は actual pole type を使う | existing pole node + request pole type が異なる route | GenerateFromBackboneSpec | Invariant: existing endpoint は既存 pole type の band height、新規 endpoint は request pole type の band height を使う | Pole.pole_type_id / Port.world_position | existing pole を request type 扱いする回帰防止 |
| C409 | Boundary | backbone | backbone は missing port band を拒否する | 対象 category/layer band を消した pole type | GenerateFromBackboneSpec | Boundary: unsupported で失敗する | EditResult.error | band 不在時に固定値や fallback で補う回帰防止 |
| C410 | Boundary | backbone | placement height は pairs に影響しない | source scan | wire_core_tests | Boundary: `pairs make(graph)` body に pole type / port band / height 参照が無い | source text | height 決定が pair/row axis 正本へ混ざる回帰防止 |
| C411 | Invariant | backbone | backbone lateral offset は row axis 方向へ動く | 2点 path を `lateral_offset_m=0/+1/-1` で比較 | GenerateFromBackboneSpec | Invariant: generated port / curve endpoint が row axis 方向に +/-1m 移動する | DetailCurve.sample_points | lateral offset の符号や方向が別 resolver で揺れる回帰防止 |
| C412 | Invariant | backbone | backbone lateral offset 符号は決定的 | fresh state 2つに同じ `lateral_offset_m=+1` | GenerateFromBackboneSpec | Invariant: port positions / curve sample points / bounds が一致する | Port / DetailCurve / BoundsCacheEntry | row axis 符号が状態順で反転する回帰防止 |
| C413 | Boundary | backbone | backbone lateral offset は pairs に影響しない | source scan | wire_core_tests | Boundary: `pairs make(graph)` body に constraints / lateral_offset_m 参照が無い | source text | placement offset が pair/row axis 正本へ混ざる回帰防止 |
| C414 | Boundary | backbone | backbone は 2点 route + 1 avoid point の simple detour を生成する | 2点 line の中央に `avoid_points` + `avoid_radius_m > 0` | GenerateFromBackboneSpec | Boundary: input route を upstream で deterministic detour へ変換し、generated node / saved graph / layout / geom を生成する | BackboneGenerationConstraints / SavedBackboneGraph | route 上 avoid point が無条件 unsupported になり viewer 操作が止まる、または downstream が geometry fallback で避ける回帰防止 |
| C415 | Boundary | backbone | backbone は空の levels 層を持たない | source scan | wire_core_tests | Boundary: `struct levels` と levels pipeline step が backbone source に無い | source text | 将来用の空箱が便利箱化する回帰防止 |
| C416 | Invariant | backbone | backbone `kNotPresent` node mode は no-op | mode 無し request と `kNotPresent` 付き request | GenerateFromBackboneSpec | Invariant: generated span count / port positions / curve sample points / bounds が一致する | Port / DetailCurve / BoundsCacheEntry | no-op 入力が生成意味へ混ざる回帰防止 |
| C417 | Boundary | backbone | backbone は target row が無い `kPassThrough` node mode を拒否する | terminal `BundleNodeMode::kPassThrough` | GenerateFromBackboneSpec | Boundary: unsupported で失敗する | EditResult.error | pass-through 意味決定を未仕様のまま受ける回帰防止 |
| C418 | Boundary | backbone | backbone は request 外 bundle の node mode を拒否する | request に無い bundle template の `kNotPresent` | GenerateFromBackboneSpec | Boundary: unsupported で失敗する | EditResult.error | no-op を broken input ignore と誤解する回帰防止 |
| C419 | Boundary | backbone | backbone は範囲外 node mode point index を拒否する | input path 範囲外 `point_index` | GenerateFromBackboneSpec | Boundary: unsupported で失敗する | EditResult.error | input point index と内部 graph node id を混同する回帰防止 |
| C420 | Boundary | backbone | backbone node mode は pairs に影響しない | source scan | wire_core_tests | Boundary: `pairs make(graph)` body に node_bundle_modes / BundleNodeMode 参照が無い | source text | node mode が pair/row axis 正本へ混ざる回帰防止 |
| C421 | Boundary | backbone | backbone topo row は row source を運ぶ | source scan | wire_core_tests | Boundary: `trow` が row/node/source/axis を持ち、`emit_ports` が `ps.rows` からコピーする | source text | topo.rows が port 実体だけを持ち、後段が row 意味を推測し始める回帰防止 |
| C422 | Boundary | backbone | backbone rules は topo と groups を読む | source scan | wire_core_tests | Boundary: rules make は `topo` と placement `groups` を入力に取り、body に `ps.links` 参照が無い | source text | rules が pairs.links の endpoint row mapping を再び読む、または lowering placement を intent 直読へ戻す回帰防止 |
| C423 | Boundary | backbone | backbone topo span は endpoint row index を運ぶ | source scan | wire_core_tests | Boundary: `tspan` が `arow/brow` を持ち、`emit_spans` が `edge.arow/brow` をコピーする | source text | span endpoint row 対応が rules 側へ漏れる回帰防止 |
| C424 | Boundary | backbone | backbone は saved backbone graph nodes/edges を保存する | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: saved graph に node 2つ、edge 1つがあり node は pole id を持つ | SavedBackboneGraph | Pole/Port/Span から backbone を復元するだけの構造へ戻る回帰防止 |
| C425 | Boundary | backbone | backbone backbone edge は複数 span を edge bundle 経由で持てる | 2点 path + multiple bundle | GenerateFromBackboneSpec | Boundary: 1 edge の `edge_bundles` 配下に generated span ids が全て入る | SavedBackboneGraph / BackboneIndex | pole 間 multiple spans を edge bundle 正本へ束ねられない回帰防止 |
| C426 | Boundary | backbone | backbone existing pole は saved graph node を再利用する | existing pole node へ追加生成 | GenerateFromBackboneSpec を2回 | Boundary: 同じ pole_id の saved node は重複せず、edge だけ増える | SavedBackboneGraph / BackboneIndex.pole_node | existing pole 接続で graph node が重複する回帰防止 |
| C427 | Boundary | backbone | backbone graph index は graph と出力を結ぶ | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: `pole_node` / `node_edges` / `edge_bundles` / `span_edge_bundle` が引ける | BackboneIndex | 変更対象収集の土台になる index が保存されない回帰防止 |
| C428 | Boundary | backbone | backbone pole frontier は saved graph incident を集める | A-B-C 生成後に B-D を追加 | CoreView::pole_frontier | Boundary: B の incident edges/spans/poles が saved graph index から取れる | BackboneFrontier / BackboneIndex | 変更対象を全 span scan や flag scan で集める回帰防止 |
| C429 | Boundary | backbone | backbone span frontier は edge bundle 配下 span を集める | 2点 path + multiple bundle | CoreView::span_frontier | Boundary: 1 span から同じ saved edge bundle 経由で spans と endpoint poles が取れる | BackboneFrontier / BackboneIndex | Span から graph edge を復元せず edge bundle を読む境界の回帰防止 |
| C430 | Boundary | backbone | backbone frontier は saved graph index を読む | source scan | wire_core_tests | Boundary: frontier 実装が `SavedBackboneEdges` / `spans_by_port` を読まず saved graph/index を読む | source text | frontier が旧 span-derived topology scan へ戻る回帰防止 |
| C431 | Boundary | backbone | backbone edge bundle は saved backbone unit | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: edge 1 / edge_bundle 1 が保存され、edge_bundle が bundle_id と span_ids を持つ | SavedBackboneEdgeBundle | backbone graph が span 直下ではなく edge-bundle 正本を持つ境界の回帰防止 |
| C432 | Boundary | backbone | backbone multiple bundle は複数 edge_bundle を作る | 2点 path + multiple bundles | GenerateFromBackboneSpec | Boundary: edge は 1、edge_bundle は bundle 数分 | SavedBackboneGraph.edge_bundles | bundle を backbone attachment として保存できない回帰防止 |
| C433 | Boundary | backbone | backbone は同じ pole pair の edge を再利用する | A-B 後に existing A-B へ追加生成 | GenerateFromBackboneSpec を2回 | Boundary: saved graph edge は 1 本のまま | BackboneIndex.edge_by_nodes | 同じ物理 segment が重複 edge になる回帰防止 |
| C434 | Boundary | backbone | backbone reverse duplicate same bundle は reject する | A-B 後に existing B-A へ同じ bundle | GenerateFromBackboneSpec を2回 | Boundary: duplicate same edge + bundle は unsupported で edge/edge_bundle/span 数は増えない | SavedBackboneEdgeBundle | reverse duplicate が span を無制限に増やす回帰防止 |
| C435 | Boundary | backbone | backbone edge metadata は duplicate reject で上書きされない | A-B 後に B-A へ同じ bundle | GenerateFromBackboneSpec を2回 | Boundary: reject 後も saved edge の route/order/dir は初回値のまま | SavedBackboneEdge | duplicate reject 時に edge 正本を後書きする回帰防止 |
| C436 | Boundary | backbone | backbone frontier は edge_bundle 経由で読む | 2点 path + multiple bundles | CoreView::pole_frontier / span_frontier | Boundary: frontier が edge_bundle ids / bundle ids / span ids を返す | BackboneFrontier / SavedBackboneEdgeBundle | frontier が edge-level span 直読みへ戻る回帰防止 |
| C437 | Boundary | backbone | backbone layout 保存は direct store | source scan | wire_core_tests | Boundary: `cache_span_layout` は `store_layout` だけを呼び、`cache_span_support_layout` / support group rebuild を呼ばない | source text | backbone layout 保存が旧 support layout/recalc 経路へ戻る回帰防止 |
| C438 | Boundary | backbone | backbone direct layout 保存は追加入力を要求しない | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: rules/layout は存在し、input_required=false | SpanLayoutRulesView / SpanLayoutState | direct layout 保存が seed/authority contract を生やす回帰防止 |
| C439 | Boundary | backbone | backbone source は support layout entrypoint を呼ばない | source scan | wire_core_tests | Boundary: `core/src/generation/backbone` に `cache_span_support_layout` / `support_layout_` が無い | source text | backbone が旧 support layout entrypoint へ戻る回帰防止 |
| C440 | Boundary | backbone | backbone は退役 owner 以外で authoritative backbone を直接読まない | source scan | wire_core_tests | Boundary: `core/src/generation/backbone` の `authoritative_.backbone` 直接アクセスは pipeline の `retire_untouched` に限定する | source text / pipeline::retire_untouched | saved graph owner 境界が崩れ、退役以外の段が内部 vector を読む回帰防止 |
| C441 | Boundary | backbone | backbone edge 保存は saved ref を返す | source scan | wire_core_tests | Boundary: `SavedBackboneEdgeRef` があり `save_backbone_edge` がそれを返す | source text | backbone が edge id だけ受け取り graph vector 検索へ戻る回帰防止 |
| C442 | Boundary | backbone | backbone edge_forward は saved ref から決める | source scan | wire_core_tests | Boundary: `save_graph` が `SavedBackboneEdgeRef.node_a/node_b` を読み、saved graph search を持たない | source text | edge direction 判定が CoreState owner 境界を越える回帰防止 |
| C443 | Boundary | backbone | backbone edge resolution 境界は duplicate reject 後も維持される | A-B 後に B-A へ同じ bundle | GenerateFromBackboneSpec を2回 | Boundary: edge は 1 本のまま、duplicate same edge + bundle は unsupported | SavedBackboneGraph / SavedBackboneEdgeBundle | API 境界変更で duplicate policy が抜ける回帰防止 |
| C444 | Boundary | backbone | backbone layout source は neutral 型を使う | source scan | wire_core_tests | Boundary: `core/src/generation/backbone` に `SpanSupportLayoutEntry` / `SupportLayout*` layout 型名が無い | source text | backbone layout 層が旧 support layout 型名へ戻る回帰防止 |
| C445 | Boundary | backbone | backbone layout 保存口は neutral entry を受ける | source scan | wire_core_tests | Boundary: `cache_span_layout(SpanLayoutEntry)` が direct store で保存する | source text | direct layout 保存口が旧 support layout signature / recalc 経路へ戻る回帰防止 |
| C446 | Boundary | backbone | backbone neutral layout 境界は生成結果を変えない | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: rules/layout/curve/bounds があり authority/required は false | SpanLayoutEntry / SpanLayoutRulesView | layout 型名変更で backbone derived outputs や no-authority 境界が崩れる回帰防止 |
| C447 | Boundary | backbone | backbone layout view は neutral layout を読む | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: generated span は `span_layout(span).has_layout()` が true | SpanLayoutView / SpanLayoutEntry | backbone の正規 layout 観測口が旧 projection API に戻る回帰防止 |
| C448 | Boundary | backbone | backbone tests は neutral layout read を使う | source scan | wire_core_tests | Boundary: backbone tests は `span_layout(` を使い `support_layout_projection(` を使わない | source text | acceptance が旧 projection 観測口を正本扱いし続ける回帰防止 |
| C449 | Boundary | backbone | backbone neutral layout read は authority を露出しない | source scan | wire_core_tests | Boundary: `CoreState::span_layout` は layout view だけを読み authority/contract/seed を読まない | source text | neutral read API が旧 authority contract を再公開する回帰防止 |
| C450 | Boundary | backbone | backbone layout state は neutral | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: generated span は `has_rules=true`, `has_layout=true`, `input_required=false` | SpanLayoutState | no-authority 観測が旧 contract API に戻る回帰防止 |
| C451 | Boundary | backbone | backbone tests は support layout contract を読まない | source scan | wire_core_tests | Boundary: `core/tests/backbone.cpp` に `support_layout_contract(` が無い | source text | backbone acceptance が旧 contract 観測口を正本扱いする回帰防止 |
| C452 | Boundary | backbone | backbone layout state は旧 contract 名を露出しない | source scan | wire_core_tests | Boundary: `SpanLayoutState` に authority/seed/contract 名が無い | source text | neutral 状態 view が旧 contract 語を再公開する回帰防止 |
| C453 | Boundary | backbone | backbone layout state は seed path を読まない | source scan | wire_core_tests | Boundary: `span_layout_state` は seed/store/contract path を呼ばない | source text | neutral 状態 view が旧 seed/contract 経路へ戻る回帰防止 |
| C454 | Boundary | backbone | backbone cache state は span layout cache 名を使う | source scan | wire_core_tests | Boundary: `CacheState` は `SpanLayoutCache span_layout_cache` を持ち `support_layout_cache` field を持たない | source text | neutral cache owner が旧 support layout field 名へ戻る回帰防止 |
| C455 | Boundary | backbone | backbone neutral layout API は span layout cache を読む | source scan | wire_core_tests | Boundary: neutral read/save API は `span_layout_cache` を使い `support_layout_cache` を使わない | source text | neutral API が旧 cache owner 名へ戻る回帰防止 |
| C456 | Boundary | backbone | backbone source は旧 layout cache 名を避ける | source scan | wire_core_tests | Boundary: `core/src/generation/backbone` に `support_layout_cache` / `support_layout_projection` / `support_layout_contract` が無い | source text | backbone production path が旧 layout cache/read 名へ戻る回帰防止 |
| C457 | Boundary | backbone | backbone layout cache 境界は生成結果を変えない | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: rules/layout/curve/bounds があり `input_required=false` | SpanLayoutCache / SpanLayoutState | cache owner rename で backbone derived outputs や no-input 境界が崩れる回帰防止 |
| C458 | Boundary | backbone | backbone は existing B に B-D を追加できる | A-B-C 後に existing B-D | GenerateFromBackboneSpec を2回 | Boundary: B-D spans だけが生成され、B frontier は 3 edges を返す | SavedBackboneGraph / BackboneFrontier | existing junction context を読めず新規 branch が unsupported に戻る回帰防止 |
| C459 | Boundary | backbone | backbone は existing B 経由の D-B-E を kind なしで追加できる | A-B-C 後に existing D-B-E | GenerateFromBackboneSpec を2回 | Boundary: D-B-E の 2 segment spans が生成され、B frontier は 4 edges を返す | pairs / SavedBackboneGraph | cross 相当入力を T/cross kind 分岐なしで扱えない回帰防止 |
| C460 | Boundary | backbone | backbone context link は emit されない | A-B-C 後に B-D | GenerateFromBackboneSpec を2回 | Boundary: span/port 増分は new route rows 分だけ | topo / trow / tspan | saved graph context を topology 生成対象として誤 emit する回帰防止 |
| C461 | Boundary | backbone | backbone same-edge request は duplicate context を skip する | A-B 後に existing A-B へ別 bundle | GenerateFromBackboneSpec を2回 | Boundary: saved edge は 1 本で edge_bundle/span だけ増える | SavedBackboneEdge / SavedBackboneEdgeBundle | same edge 追加が context duplicate で ambiguous/reject される回帰防止 |
| C462 | Boundary | backbone | backbone existing context は junction kind label を追加しない | source scan | wire_core_tests | Boundary: duplicate context skip と `is_new` emit gate があり kind label guard も維持 | source text | existing junction slice が T/cross/branch 分岐として再発する回帰防止 |
| C463 | Boundary | backbone | backbone duplicate same edge + bundle は reject する | A-B 後に existing A-B へ同じ bundle | GenerateFromBackboneSpec を2回 | Boundary: duplicate は unsupported で edge/edge_bundle/span 数は増えない | SavedBackboneEdgeBundle | 同一 edge + bundle の追加生成が無制限に span を増やす回帰防止 |
| C464 | Boundary | backbone | backbone different bundle は同じ edge に追加できる | A-B 後に existing A-B へ別 bundle | GenerateFromBackboneSpec を2回 | Boundary: edge は 1 本、edge_bundle は X/Y の 2 つになる | SavedBackboneEdgeBundle.bundle_id | duplicate policy が別 bundle 追加まで拒否する回帰防止 |
| C465 | Boundary | backbone | backbone duplicate policy は existing span を読まない | source scan | wire_core_tests | Boundary: duplicate 判定は saved edge_bundle を読み、existing span/layout/seed を読まない | source text | duplicate 検出が span resolution/差分補完へ滑る回帰防止 |
| C466 | Boundary | backbone | backbone duplicate reject は state を変えない | A-B 後に existing A-B へ同じ bundle | GenerateFromBackboneSpec を2回 | Boundary: reject 後の saved graph frontier / object count / edge_bundle count が reject 前と一致する | SavedBackboneGraph / BackboneFrontier | unsupported path が途中まで topology/cache を生成する回帰防止 |
| C467 | Boundary | backbone | backbone row-port binding を保存する | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: saved graph に generated port と row key の binding が保存され、port id から引ける | SavedBackbonePortBinding | 将来 port resolution が座標推測に戻る回帰防止 |
| C468 | Boundary | backbone | backbone existing context 追加でも new row-port binding だけ保存する | A-B-C 後に B-D | GenerateFromBackboneSpec を2回 | Boundary: B-D の generated spans 分だけ port binding が増え、context-only row は port binding を作らない | SavedBackbonePortBinding / topo.rows | context row を materialized port と誤って結びつける回帰防止 |
| C469 | Boundary | backbone | backbone row-port duplicate は resolve せず reject する | A-B 後に existing A-B へ同じ bundle | GenerateFromBackboneSpec を2回 | Boundary: duplicate request は unsupported で port binding 数は増えない | SavedBackbonePortBinding | existing port resolution 仕様なしに binding を重複作成する回帰防止 |
| C470 | Boundary | backbone | backbone row-port identity は位置近似を使わない | source scan | wire_core_tests | Boundary: binding 保存口は edge_bundle / row_key / lane を読み、world position / layout / seed を読まない | source text | port identity が座標近似や layout 依存に戻る回帰防止 |
| C471 | Boundary | backbone | backbone は compatible scope の saved port binding を共有できる | 3点 route | GenerateFromBackboneSpec | Boundary: 同じ middle row port に複数 binding があり、bundle/kind/layer scope が一致する | SavedBackbonePortBinding / Port | port resolution が row_key/lane だけの cross-bundle identity へ戻る回帰防止 |
| C472 | Boundary | backbone | backbone port resolution は saved binding を要求する | binding なし existing poles + 近い manual ports | GenerateFromBackboneSpec | Boundary: 近い port があっても binding が無ければ resolve しない | SavedBackbonePortBinding / Port | 座標近似で v1 由来 port を拾う回帰防止 |
| C473 | Boundary | backbone | backbone shared compatible port は span endpoint に使われる | 3点 route | GenerateFromBackboneSpec | Boundary: middle row の同じ port id が隣接 span endpoint から参照される | Span.port_a_id / port_b_id | materialized row port identity が span endpoint へ反映されない回帰防止 |
| C474 | Boundary | backbone | backbone port resolution は ambiguous binding を reject する | source scan | wire_core_tests | Boundary: 複数の有効 port binding が同じ row_key/lane に割れた場合は unsupported | source text | 曖昧な resolution を恣意的に選ぶ回帰防止 |
| C475 | Boundary | backbone | backbone port resolution は existing layout を読まない | source scan | wire_core_tests | Boundary: resolution 判定は row-port binding を読み、support layout / seed / world position を読まない | source text | port resolution が layout/materialization/位置推測に戻る回帰防止 |
| C476 | Boundary | backbone | backbone branch row は kind なしで高さだけ分離され、HV bundle の水平中心を動かさない | A-B-C 後に HV の B-D | GenerateFromBackboneSpec を2回 | Boundary: B の materialized branch row ports は context main row ports と同位置にならず、HV 3相の水平重心は pole 中心と一致し、kind label は作らない | port placement / source text | branch 相当入力で row が潰れる、または row 分離が bundle 中心を横へ動かす回帰防止 |
| C477 | Boundary | backbone | backbone cross row は kind なしで高さだけ分離される | A-B-C 後に D-B-E | GenerateFromBackboneSpec を2回 | Boundary: B の materialized cross row ports は context row ports と同位置にならず、cross kind label は作らない | port placement / source text | cross 相当入力で row が潰れる、または cross enum が再発する回帰防止 |
| C478 | Invariant | backbone | backbone row height separation は deterministic | A-B-C 後に B-D を fresh state 2回 | GenerateFromBackboneSpec を2回 | Invariant: port positions / curve points / bounds が一致する | port placement / curve / bounds | row height separation が unordered context や生成順で揺れる回帰防止 |
| C479 | Boundary | backbone | backbone row height separation は pairs を変えない | source scan | wire_core_tests | Boundary: `pairs make(graph)` は row height separation 定数/helper を読まない | source text | port placement policy が pair/open/row source 決定へ混ざる回帰防止 |
| C480 | Boundary | backbone | backbone context rows は高さ順序に入るが emit されない | A-B-C 後に B-D + source scan | GenerateFromBackboneSpec / wire_core_tests | Boundary: context rows を含む `row_height_offsets(ps)` を使い、active row 以外の ports は作らない | topo.rows / port count / source text | context-only row が port/span として materialize される回帰防止 |
| C481 | Boundary | backbone | backbone pass-through mode は限定範囲で受ける | A-B-C 後に saved B から B-D + invalid modes | GenerateFromBackboneSpec | Boundary: saved junction node の kPassThrough は通り、範囲外 point / unknown bundle は reject | BackboneSpec.node_bundle_modes | node mode を無制限に受けて pair/row 決定へ混ぜる回帰防止 |
| C482 | Boundary | backbone | backbone pass-through は explicit intent を保存する | A-B-C 後に saved B から B-D + kPassThrough | GenerateFromBackboneSpec | Boundary: generated span rules/layout に lower/pass intent が保存され、pair 決定は変わらない | SpanLayoutRule / SpanLayoutEntry | pass-through 指定が保存されず後段推測へ戻る回帰防止 |
| C483 | Boundary | backbone | backbone pass-through ambiguous target は reject する | source scan | wire_core_tests | Boundary: target row が一意でなければ unsupported にする分岐がある | source text | geometry や順序で曖昧な target row を選ぶ回帰防止 |
| C484 | Boundary | backbone | backbone lowering draw は layout だけを読む | A-B-C 後に saved B から B-D + kPassThrough | GenerateFromBackboneSpec | Boundary: lowering span に visual/render cache があり、draw は lowering/topology を再判断しない | SpanVisualCache / SpanRenderCache / SpanLayoutEntry | draw が lowering/topology を再判断する回帰防止 |
| C485 | Boundary | backbone | backbone lowering intent は existing span を読まない | source scan | wire_core_tests | Boundary: intent 決定は node_bundle_modes と pairs を読み、existing span/layout/seed を読まない | source text | intent 決定が v1/recalc/materialization へ戻る回帰防止 |
| C486 | Invariant | backbone | backbone pass-through intent は deterministic | A-B-C 後に B-D + kPassThrough を fresh state 2回 | GenerateFromBackboneSpec を2回 | Invariant: intents / port positions / curve points / bounds が一致する | SpanLayoutRule / port placement / bounds | pass-through intent が saved graph traversal 順で揺れる回帰防止 |
| C487 | Boundary | backbone | backbone port resolution は bundle-compatible scope を要求する | A-B 後に同じ edge へ別 bundle | GenerateFromBackboneSpec を2回 | Boundary: 異なる bundle/kind/layer の generated span は既存 port を共有しない | SavedBackbonePortBinding / Port | row_key/lane だけで異種 bundle port を共有する回帰防止 |
| C488 | Boundary | backbone | backbone compatible binding は同じ port identity を共有できる | 3点 route | GenerateFromBackboneSpec | Boundary: 同じ port の複数 binding は bundle/kind/layer が一致する | SavedBackbonePortBinding / Port | compatible scope の明示なしに binding を別 port へ割る回帰防止 |
| C489 | Boundary | backbone | backbone port binding index は全 binding を返す | 3点 route | CoreView::backbone_port_bindings_for_port | Boundary: 複数 binding を持つ port で vector index が全 binding を返し、scope が一致する | BackboneIndex.port_bindings_by_port | 複数 binding を singular index で上書きする回帰防止 |
| C490 | Boundary | backbone | backbone duplicate same edge/bundle/lane は重複生成しない | A-B 後に existing A-B へ同じ bundle | GenerateFromBackboneSpec を2回 | Boundary: duplicate は unsupported で state unchanged | SavedBackboneGraph / object counts | duplicate request が span/port を増やす回帰防止 |
| C491 | Invariant | backbone | backbone branch lowering v1 は geom に反映される | A-B-C 後に B-D + kPassThrough | GenerateFromBackboneSpec を2回 | Invariant: lowered endpoint / curve sample / bounds が port height より下がる | SpanLayoutEntry / DetailCurve / BoundsCacheEntry | lowering intent が保存だけで geom に反映されない回帰防止 |
| C492 | Invariant | backbone | backbone cross lowering v1 は new links だけに反映される | A-B-C 後に D-B-E + kPassThrough | GenerateFromBackboneSpec を2回 | Invariant: emitted span は new route 分だけで、generated geom は lowered を反映する | topo / SpanLayoutEntry / DetailCurve | context link を再生成する、または lowering を context 出力へ混ぜる回帰防止 |
| C493 | Boundary | backbone | backbone pass-through は pair/open を変えない | source scan | wire_core_tests | Boundary: node mode / separation / lowering は `pairs make(graph)` に入らない | source text | pass-through が connectivity authority へ混ざる回帰防止 |
| C494 | Boundary | backbone | backbone lowering v1 draw は再判断しない | A-B-C 後に B-D + kPassThrough | GenerateFromBackboneSpec を2回 | Boundary: lowering draw は C484 と同じ layout/geom consumer として動く | SpanVisualCache / SpanRenderCache | draw が lowering intent を新規判断する回帰防止 |
| C495 | Boundary | backbone | backbone lowering v1 は existing span を読まない | source scan | wire_core_tests | Boundary: intent/lowering 決定は saved graph context と node modes を読み、span/layout/seed を読まない | source text | lowering が v1/recalc/materialization へ戻る回帰防止 |
| C496 | Invariant | backbone | backbone junction v1 は deterministic | A-B-C 後に B-D + kPassThrough を fresh state 2回 | GenerateFromBackboneSpec を2回 | Invariant: saved graph / port bindings / intents / port positions / curve / bounds が一致する | SavedBackboneGraph / SpanLayoutEntry / BoundsCacheEntry | existing junction output が traversal/order で揺れる回帰防止 |
| C497 | Boundary | backbone | backbone context rows は order に使うが materialize しない | A-B-C 後に B-D | GenerateFromBackboneSpec を2回 | Boundary: context rows は ordering 入力、port/span は generated rows のみ | topo.rows / ports / spans | context-only row を port/span として生成する回帰防止 |
| C498 | Boundary | backbone | backbone saved graph は topology authority のまま | A-B-C 後に B-D + kPassThrough | CoreView::pole_frontier | Boundary: frontier は expected edges/edge_bundles/spans を saved graph から返す | SavedBackboneGraph / BackboneFrontier | topology を spans/layout/seed から復元する構造へ戻る回帰防止 |
| C499 | Boundary | backbone | backbone context link は save target ではない | A-B-C 後に B-D | GenerateFromBackboneSpec を2回 | Boundary: saved graph edge は A-B / B-C / B-D の3本だけで、context A-B/B-C は再保存されない | SavedBackboneGraph / BackboneFrontier | context link が `save_backbone_edge` 対象になり new link と混ざる回帰防止 |
| C500 | Boundary | backbone | backbone context link は saved edge ref を要求する | source scan | wire_core_tests | Boundary: `ref_for_existing_edge` は `edge.saved` だけを読み、invalid context は unsupported | source text | context link を node pair / geometry / span/layout から推測して保存する回帰防止 |
| C501 | Boundary | backbone | backbone Gate 3 契約は save_graph でも維持される | source scan | wire_core_tests | Boundary: `save_graph` は `edge.is_new` gate の内側だけで `save_backbone_edge` を呼び、context は existing ref を読む | source text | emit では分離していても save 境界で context/new が混ざる回帰防止 |
| C502 | Boundary | backbone | backbone span bindingはlane identityを保存し、edge bundleをpair/id indexで参照する | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: generated spanごとに`SavedBackboneSpanBinding`がありlaneが一意で、`(edge_id,bundle_id)`と`edge_bundle_id`から保存位置を直接解決できる | SavedBackboneSpanBinding / BackboneIndex | edge_bundle.span_idsだけでlaneが分からない、またはsave_graphがbundle/spanごとに全edge_bundlesを線形探索する回帰防止 |
| C503 | Boundary | backbone | backbone duplicate span binding lane は reject する | source scan | wire_core_tests | Boundary: `bind_backbone_span` は同じ edge_bundle/lane を `duplicate backbone span binding` として拒否する | SavedBackboneSpanBinding | same edge_bundle + lane の span が複数保存される回帰防止 |
| C504 | Boundary | backbone | backbone span resolution は geometry/layout を読まない | source scan | wire_core_tests | Boundary: `bind_backbone_span` は saved binding/lane を読み、span_layout/seed/curve/world_position を読まない | source text | span resolution が curve 類似・layout・位置推測へ滑る回帰防止 |
| C505 | Boundary | backbone | backbone save_graph は span binding failure を伝播する | source scan | wire_core_tests | Boundary: `save_graph` は `span.lane` を渡し、`bind_backbone_span` failure を返す | source text | duplicate span binding を保存境界で黙殺する回帰防止 |
| C506 | Boundary | backbone | backbone support group は placement layer | source scan | wire_core_tests | Boundary: `group` は row_members / group_axis / vertical_order / lower_offset_m を持つ | source text | support group が便利箱化して topology/visual 責務を持つ回帰防止 |
| C507 | Boundary | backbone | backbone support group は intent 後に作る | source scan | wire_core_tests | Boundary: `make(ps, intents)` で groups を作り、rules は groups を読む | source text | lowering placement が intent/rules/layout に分散する回帰防止 |
| C508 | Boundary | backbone | backbone support group が lowered rules を駆動する | A-B-C 後に B-D + kPassThrough | GenerateFromBackboneSpec | Boundary: lowered endpoint は support_group_id と branch_down_offset を持つ | SpanLayoutRule / EndpointLayoutRule | lowering が group を経由せず rules に直書きされる回帰防止 |
| C509 | Boundary | backbone | backbone support group は visual 語を持たない | source scan | wire_core_tests | Boundary: group 型に mount/tip/arm/insulator/attachment が無い | source text | support group が draw/support visual の責務を先取りする回帰防止 |
| C510 | Boundary | backbone | backbone layout は fixture placement plan を読む | source scan | wire_core_tests | Boundary: ApplyEndpointLayoutRule は endpoint_world を後段で下げず、derive_span_layout は FixturePlacementPlanByPort の wire_endpoint を読み、fallback だけ endpoint_down_offset を使う | source text | layout が placement plan を迂回する、または socket 解決後に lowering を二重適用する回帰防止 |
| C511 | Boundary | backbone | backbone draw は geom から保存される | 2点 path + single bundle | GenerateFromBackboneSpec | Boundary: visual/render cache が存在し、render 距離配列は curve samples に対応する | SpanRenderCacheEntry / CurveCacheEntry | draw が geom 以外の経路や未保存状態へ戻る回帰防止 |
| C512 | Boundary | backbone | backbone draw は topology を読まない | source scan | wire_core_tests | Boundary: draw maker body が pairs/backbone/node modes/save bindings を読まない | source text | draw が topology / pair / row / lowering を再判断する回帰防止 |
| C513 | Boundary | backbone | backbone lowered layout は obsolete SupportArm を出さない | A-B-C 後に B-D + kPassThrough | GenerateFromBackboneSpec | Boundary: lowered layout を持つ span でも SpanVisualCacheEntry の parts は空 | SpanVisualCacheEntry / SpanLayoutEntry | loweringをmodel/socket配置で消費した後に旧SupportArmが復活する回帰防止 |
| C514 | Boundary | backbone | backbone draw save は direct store | source scan | wire_core_tests | Boundary: `cache_span_visual` / `cache_span_render` は direct cache store で rebuild 経路を呼ばない | source text | draw 保存が recalc/rebuild 経路へ戻る回帰防止 |
| C515 | Boundary | backbone | backbone は saved graph の無い existing pole を拒否する | 手動作成 pole を node_specs に指定 | GenerateFromBackboneSpec | Boundary: unsupported で失敗し、saved graph / objects は増えない | EditResult.error / SavedBackboneGraph | v1/手動 scene を暗黙 migration する回帰防止 |
| C516 | Boundary | backbone | backbone 生成済み pole は existing node として使える | A-B-C 後に saved B から B-D | GenerateFromBackboneSpec を2回 | Boundary: saved graph node を持つ pole は追加 route へ接続できる | SavedBackboneGraph / BackboneFrontier | M29 gate が backbone 正規 saved graph context まで拒否する回帰防止 |
| C517 | Boundary | backbone | backbone migration gate は outputs から推測しない | source scan | wire_core_tests | Boundary: prepare は `backbone_node_for_pole` を要求し、span/layout/curve/bounds/save node を読まない | source text | span/layout/seed/curve/position から graph を復元する回帰防止 |
| C518 | Boundary | backbone | backbone lowered layout は support_world と endpoint_world を最終fixture高さへ一致させる | A-B-C 後に B-D + kPassThrough | GenerateFromBackboneSpec を2回 | Boundary: lowered endpoint は positive down offsetを保持しつつ `support_world == endpoint_world` でport論理anchorより下がる | SpanLayoutEntry / Port | modelを元高さに残し線端だけを下げる旧契約の復活を防ぐ |
| C519 | Boundary | backbone | backbone draw は lowering placeholder を出さない | A-B-C 後に B-D + kPassThrough + source scan | GenerateFromBackboneSpec / wire_core_tests | Boundary: lowered endpoint は `support_world == endpoint_world` で、draw makerはSupportArm partを出さない | SpanVisualCacheEntry / SpanLayoutEntry / source text | SupportArmで旧support/endpoint差を埋める経路の復活を防ぐ |
| C520 | Boundary | backbone | backbone duplicate span binding preflight は emit 前に走る | source scan | wire_core_tests | Boundary: build は `check(ps)` を `emit(ps)` より前に呼び、preflight は saved span binding を読んで AddPort/AddSpan/AddBundle を呼ばない | source text / SavedBackboneSpanBinding | duplicate reject が生成後 rollback や保存時 invariant だけに依存する回帰防止 |
| C521 | Boundary | backbone | backbone context link は saved dir を保つ | source scan | wire_core_tests | Boundary: prepare は new link と context link の dir を graph に入れ、pairs make は node geometry から dir を再代入しない | SavedBackboneEdge.dir / source text | context link の保存済み向きを connectivity 段で node geometry から再生成する回帰防止 |
| C522 | Boundary | backbone | backbone supported scope は文書化されている | docs/architecture.md source scan | wire_core_tests | Boundary: supported / unsupported / topology / connectivity / duplicate / layout-draw 境界が明記されている | docs/architecture.md | supported scope が暗黙化し、実装判断が場当たりになる回帰防止 |
| C523 | Boundary | backbone | backbone scope gate は entrypoint と一致する | source scan | wire_core_tests | Boundary: GenerateFromBackboneSpec は backbone pipeline の prepare/check/build を呼び、v1 pipeline へ戻らず、duplicate preflight は emit 前に走る | GenerateFromBackboneSpec / backbone build order | supported scope freeze と入口実装がズレる回帰防止 |
| C524 | Boundary | backbone | backbone simple line scenario は mainline 出力と authority を見る | 2点 path + source scan | GenerateFromBackboneSpec / wire_core_tests | Boundary: rules/layout/geom/draw/saved graph が揃い、backbone source は v1 deps を読まない | generated outputs / SavedBackboneGraph / source text | 単純線が結果だけ通り、実際は v1/recalc に戻る回帰防止 |
| C525 | Boundary | backbone | backbone polyline scenario は connectivity 一点決定を守る | 3点 path | GenerateFromBackboneSpec | Boundary: link/pair/open/row model が成立し、rules は topology/group を consume する | pairs / rules | polyline 対応で downstream が connectivity を再判断する回帰防止 |
| C526 | Boundary | backbone | backbone multiple bundle scenario は connectivity を共有する | 3点 path + 複数 bundle | GenerateFromBackboneSpec | Boundary: 複数 bundle output が揃い、bundle spec は pair を変えない | pairs / topology outputs | bundle spec ごとに pair/open/row を分岐させる回帰防止 |
| C527 | Boundary | backbone | backbone existing pole continuation scenario は saved graph だけを見る | saved graph pole / manual pole / existing span | GenerateFromBackboneSpec | Boundary: saved graph pole は接続でき、manual pole は reject、existing span は意味入力にしない | SavedBackboneGraph / EditResult | existing 接続が saved graph ではなく span/layout/seed 推測へ戻る回帰防止 |
| C528 | Boundary | backbone | backbone branch scenario は new link だけ emit する | A-B-C 後に B-D | GenerateFromBackboneSpec を2回 | Boundary: B-D だけ生成され、context link は emit/save target ではない | topo / SavedBackboneGraph | context link を生成・保存対象に混ぜる回帰防止 |
| C529 | Boundary | backbone | backbone cross scenario は kind label なしで通る | A-B-C 後に D-B-E | GenerateFromBackboneSpec を2回 | Boundary: cross 相当は pair+pair として通り、cross kind label を作らない | pairs / source text | T/cross/branch enum が再発する回帰防止 |
| C530 | Boundary | backbone | backbone same-edge different-bundle scenario は edge を共有する | A-B 後に同じ edge へ別 bundle | GenerateFromBackboneSpec を2回 | Boundary: saved edge は共有し、edge_bundle/port scope は bundle-compatible に分かれる | SavedBackboneEdge / SavedBackboneEdgeBundle / PortBinding | 同じ edge の bundle 追加が duplicate 扱いになる、または異種 port を共有する回帰防止 |
| C531 | Boundary | backbone | backbone duplicate scenario は mutation なしで reject する | A-B 後に同じ edge/bundle/lane | GenerateFromBackboneSpec を2回 | Boundary: duplicate は preflight で reject し、state は unchanged | SavedBackboneSpanBinding / object counts | duplicate reject が生成後 rollback 前提になる回帰防止 |
| C532 | Boundary | backbone | backbone pass-through lowering scenario は consumer chain を守る | A-B-C 後に B-D + kPassThrough | GenerateFromBackboneSpec を2回 | Boundary: pair/open は変わらず、layout/geom/draw だけが lowering result を consume する | pairs / SpanLayoutEntry / DetailCurve / draw cache | pass-through が connectivity を変える、または draw が lowering を再判断する回帰防止 |
| C533 | Boundary | backbone | backbone build mutation order は固定されている | source scan | wire_core_tests | Boundary: pairs→duplicate preflight→intent→groups→emit→save_graph→rules→layout→geom→draw の順で走る | backbone pipeline::build | mutation 前に分かる失敗が emit 後へ戻る回帰防止 |
| C534 | Boundary | backbone | backbone invalid inputs は emit 前に止まる | missing band / avoid / saved graph missing | GenerateFromBackboneSpec | Boundary: request/template/saved graph から分かる unsupported は topology emission 前に失敗する | check / prepare / preflight | unsupported 入力が途中まで object を生成する回帰防止 |
| C535 | Boundary | backbone | backbone duplicate preflight は mutation boundary | duplicate same edge/bundle/lane + source scan | GenerateFromBackboneSpec / wire_core_tests | Boundary: duplicate reject は state unchanged で、preflight は AddPort/AddSpan/AddBundle を呼ばない | SavedBackboneSpanBinding / source text | duplicate reject が保存時 invariant や rollback に依存する回帰防止 |
| C536 | Boundary | backbone | backbone draw consumer output は最小 | 2点 path / lowered branch | GenerateFromBackboneSpec | Boundary: draw は geom renderを保存し、lowering用SupportArmは保存しない | SpanRenderCacheEntry / SpanVisualCacheEntry / SpanLayoutEntry | draw がmodel/socketで解決済みのloweringを再解釈する回帰防止 |
| C537 | Boundary | backbone | backbone draw source は decision input を読まない | source scan | wire_core_tests | Boundary: draw maker は state/saved graph/pairs/edge_bundle/support_group/branch_down_offset を読まない | backbone draw maker source | draw が topology/connectivity/lowering を再判断する回帰防止 |
| C538 | Boundary | backbone | backbone viewer deps は core draw gate ではない | docs/architecture.md source scan | wire_core_tests | Boundary: viewer/raylib deps は core acceptance blocker ではないと文書化されている | docs/architecture.md | viewer 環境不備で core backbone acceptance を止める回帰防止 |
| C539 | Boundary | backbone | backbone supported request は saved graph output を作る | 2点 path + source scan | GenerateFromBackboneSpec / wire_core_tests | Boundary: supported request は backbone entrypoint から saved graph/rules/layout/geom/draw を作る | GenerateFromBackboneSpec / SavedBackboneGraph | supported scope が v1 fallback や未保存 graph に戻る回帰防止 |
| C540 | Boundary | backbone | backbone unsupported request は v1 output を作らない | empty bundles | GenerateFromBackboneSpec | Boundary: unsupported は失敗し、pole/port/bundle/span/saved graph counts が変わらない | EditResult / object counts | unsupported が v1-style output を途中生成する回帰防止 |
| C541 | Boundary | backbone | backbone manual existing pole without graph は gate reject | manual pole node_spec | GenerateFromBackboneSpec | Boundary: saved graph の無い existing pole は reject され、outputs から graph を推測しない | SavedBackboneGraph / source text | v1/manual scene を暗黙 migration する回帰防止 |
| C542 | Boundary | backbone | backbone usable mainline architecture audit は pass | docs + aggregate acceptance | wire_core_tests | Boundary: topology/connectivity/placement/duplicate/draw/gate owner が docs と acceptance で一致する | docs/architecture.md / backbone tests | usable mainline 化の境界がまた曖昧になる回帰防止 |
| C543 | Boundary | backbone | backbone は新規 route interior の `kPassThrough` を supported scenario として通す | 3点 route + middle point kPassThrough | GenerateFromBackboneSpec | Boundary: saved junction context なしでも current route の一意な pair row だけで intent/layout/geom/draw を生成する | pairs / SpanLayoutEntry / DetailCurve / SavedBackboneGraph | C番号ではなく supported scenario が増えたことを固定し、saved graph 限定 gate へ戻る回帰防止 |
| C544 | Boundary | backbone | backbone は generated pole pinning を supported scenario として反映する | `pin_endpoints` / `pin_vertices` | GenerateFromBackboneSpec | Boundary: 新規生成 pole の placement mode だけが変わり、existing pole / topology / connectivity は変わらない | Pole.placement_mode / generated_pole_ids | pole placement 入力を無視する、または existing pole を副作用で manual 化する回帰防止 |
| C545 | Boundary | backbone | backbone は `interval_m` で中間 pole を生成する | 2点 route + `interval_m=4` | GenerateFromBackboneSpec | Boundary: interval node は同じ graph pipeline の generated pole/span になり、`pin_vertices` は original clicked vertices にだけ効く | SavedBackboneGraph / Pole.placement_mode / generated_span_ids | interval 入力を無視し続ける、または auto interval node を manual vertex 扱いする回帰防止 |
| C546 | Boundary | backbone | backbone は explicit new-pole node spec を supported scenario として受ける | `SupportKind::kPole` + invalid node_id | GenerateFromBackboneSpec | Boundary: 明示 new pole marker は generated pole になり、missing existing pole id は引き続き reject | BackboneInputSpec.node_specs / SavedBackboneGraph / C398 | explicit new pole 指定を壊れた existing id と誤判定する、または不正 id を新規 pole fallback する回帰防止 |
| C547 | Boundary | backbone | backbone は fixed template の exact count 指定を supported scenario として受ける | fixed-count bundle + `count == fixed_count` | GenerateFromBackboneSpec | Boundary: 冗長な exact count は no-op として生成でき、異なる count は mutation 前 reject | BackboneBundleSpec.count / BundleTemplate.fixed_count / generated_span_ids | UI が固定数を明示しただけで reject する、または mismatched count を lane 数変更として受ける回帰防止 |
| C548 | Boundary | backbone | backbone は avoid radius 単独指定を no-op supported scenario として受ける | `avoid_radius_m > 0` + empty `avoid_points` | GenerateFromBackboneSpec | Boundary: 避ける点が無い radius は生成意味に影響せず通る | BackboneGenerationConstraints | radius だけの UI 入力を不必要に reject する回帰防止 |
| C549 | Boundary | backbone | backbone は range template の explicit count を supported scenario として受ける | Communication `count=3` | GenerateFromBackboneSpec | Boundary: min/max 内 count は実 lane/span 数として生成し、範囲外 count は mutation 前 reject | BackboneBundleSpec.count / BundleTemplate.min_count,max_count / Bundle.count | range count を無視する、または範囲外 count をそのまま生成する回帰防止 |
| C550 | Boundary | backbone | backbone は generated pole の tangent hint を supported scenario として反映する | explicit new-pole node spec + non-zero tangent | GenerateFromBackboneSpec | Boundary: tangent は generated pole yaw だけを変え、zero tangent は mutation 前 reject | BackboneInputSpec.node_specs / Pole.world_transform.rotation_euler_deg | tangent hint を無視する、または topology/connectivity/row placement に混ぜる回帰防止 |
| C551 | Boundary | backbone | backbone は missing pole type を bundle template から解決する | `pole_type_id` invalid + Communication bundle | GenerateFromBackboneSpec | Boundary: 単一 related pole type は generated pole に適用され、mixed related pole type は mutation 前 reject | BundleTemplate.related_pole_type_id / Pole.pole_type_id | pole type 未指定 UI 入力を不必要に reject する、または混在 bundle から pole type を推測する回帰防止 |
| C552 | Boundary | backbone | backbone は zero-radius avoid points を no-op supported scenario として受ける | `avoid_points` + `avoid_radius_m == 0` | GenerateFromBackboneSpec | Boundary: disabled avoid payload は layout/geom を変えない | BackboneGenerationConstraints / DetailCurve.sample_points | disabled avoid UI 入力を不必要に reject する回帰防止 |
| C553 | Boundary | backbone | backbone は new midair route point を supported scenario として受ける | 3点 route + middle `SupportKind::kMidair` | GenerateFromBackboneSpec | Boundary: midair 点は pole を作らず saved backbone node / ownerless port / span output を作る。existing midair は引き続き reject | SavedBackboneGraph / Port.owner_pole_id / generated_pole_ids / C397 | midair 入力を v1 fallback へ戻す、または pole として誤生成する回帰防止 |
| C554 | Boundary | backbone | backbone は existing saved midair route point を supported scenario として受ける | new midair 生成後、その saved node id から extension | GenerateFromBackboneSpec | Boundary: saved backbone node id だけで existing midair を解決し、pole を作らず ownerless port/span を生成する | SavedBackboneNode / Port.owner_pole_id / generated_pole_ids / C397 | existing midair を span/layout/座標近似から推測する、または pole として誤生成する回帰防止 |
| C555 | Boundary | backbone | backbone は new building route point を supported scenario として受ける | 3点 route + middle `SupportKind::kExternal` | GenerateFromBackboneSpec | Boundary: building 点は pole を作らず saved backbone node / ownerless port / span output を作る | SavedBackboneGraph / SavedBackboneNode.support_kind / Port.owner_pole_id / generated_pole_ids | building 入力を v1 fallback へ戻す、または pole として誤生成する回帰防止 |
| C556 | Boundary | backbone | backbone は building pick を new building route point として受ける | `PickHitKind::kExternal` の ResolveBranchPick 結果を node_specs に渡す | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: building hit id を saved node id として使わず、ownerless building node / port / span output を作る | ResolveBranchPickResult / SavedBackboneGraph / Port.owner_pole_id | viewer building pick が Pole 扱いや saved node id 推測へ戻る回帰防止 |
| C557 | Boundary | backbone | backbone は object id の無い building pick を受ける | `PickHitKind::kExternal` + invalid hit_id | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: building pick は位置を new building route point として使い、saved node id を要求しない | ResolveBranchPickResult / SavedBackboneGraph | viewer raycast が stable building id を持たない場合に不必要に reject する回帰防止 |
| C558 | Boundary | backbone | backbone は ground pick を new ground route point として受ける | `PickHitKind::kGround` の ResolveBranchPick 結果を node_specs に渡す | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: ground 点は pole/anchor を作らず saved backbone node / ownerless port / span output を作る | ResolveBranchPickResult / SavedBackboneGraph / Port.owner_pole_id | ground pick を v1 fallback へ戻す、または attachment/anchor 仕様なしに推測生成する回帰防止 |
| C559 | Boundary | backbone | backbone は route と交差しない positive avoid を no-op として受ける | avoid point が route から十分離れている `avoid_radius_m > 0` | GenerateFromBackboneSpec | Boundary: clear avoid は layout/geom を変えず通る | BackboneGenerationConstraints / DetailCurve.sample_points | clear avoid が不要に route を変える回帰防止 |
| C560 | Boundary | backbone | backbone は selected bundle 未指定の segment dry-run pick を midair route point として受ける | `PickHitKind::kSegment` + `create_midair_node=false` + selected bundle 未指定 | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: bundle policy 未指定の dry-run pick は ownerless midair route point になり、selected bundle がある場合の policy は維持する | ResolveBranchPickResult / SavedBackboneGraph / Port.owner_pole_id | UI の path point 解決を bundle 選択前に不必要に止める、または span/layout/seed から topology を推測する回帰防止 |
| C561 | Boundary | backbone | backbone は selected bundle 未指定の既定 segment pick を ownerless midair route point として受ける | `PickHitKind::kSegment` + default ResolveBranchPickOptions + selected bundle 未指定 | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: bundle policy 未指定の既定 pick は debug virtual node を作らず ownerless midair route point になり、selected bundle がある場合の policy は維持する | ResolveBranchPickResult / SavedBackboneGraph / Port.owner_pole_id | UI が bundle 選択前の既定 pick を virtual node id として返し、backbone が saved node 不在で reject する回帰防止 |
| C562 | Boundary | backbone | backbone は saved midair node pick から延長できる | `PickHitKind::kNode` + saved ownerless midair node id | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: node pick は saved ownerless backbone node の support kind / position を読み、span/layout/seed/position proximity から topology を推測しない | ResolveBranchPickResult / SavedBackboneGraph / Port.owner_pole_id | saved midair node を Pole と誤解して backbone extension が saved graph missing で止まる回帰防止 |
| C563 | Boundary | backbone | backbone は saved ownerless span endpoint へ segment pick から snap できる | backbone saved midair span + endpoint id の無い `PickHitKind::kSegment` | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: span id から saved graph binding を読み endpoint node を解決し、span/layout/seed/position proximity から topology を推測しない | ResolveBranchPickResult / SavedBackboneGraph / span_edge_bundle | viewer segment pick payload が endpoint ids を持たない場合に ownerless endpoint snap が効かず、backbone extension が止まる回帰防止 |
| C564 | Boundary | backbone | backbone は selected bundle 付き segment pick の transient midair node から生成できる | backbone span 中間 pick + selected LV bundle | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: pick が返した pending support node を route input として読み、新規 saved ownerless node / port / span output を作る。saved graph 既存 topology としては扱わない | ResolveBranchPickResult / pending_support_node / SavedBackboneGraph | selected bundle 指定時だけ virtual node id が返り、backbone が saved node 不在で止まる回帰防止 |
| C565 | Boundary | backbone | backbone は mixed selected midair branch で許可 bundle だけ生成する | backbone span 中間 pick + selected LV/HV bundle | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: pending support node の bundle mode は materialization 対象選択だけに使い、LV だけを bundle/span output にする | ResolveBranchPickResult / BundleTemplate.allow_midair_branch / generated bundle_ids | 禁止 bundle を巻き込んで生成する、または mixed request 全体を reject する回帰防止 |
| C566 | Boundary | backbone | backbone は全 bundle disallowed の selected midair branch を no-op にする | backbone span 中間 pick + request HV only | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: active bundle が無い場合は成功 no-op で topology/cache/object count を変えない | ResolveBranchPickResult / object counts / SavedBackboneGraph | 禁止 bundle だけの request が途中まで pole/port/span を生成する回帰防止 |
| C567 | Boundary | backbone | backbone は segment pick midair branch で source projection を使う | backbone span 中間 pick は z=0、source span は elevated endpoint を持つ | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: midair route point / ownerless branch endpoint は source projection に揃い、topology は saved graph/pick input からだけ読む | ResolveBranchPickResult.position / Port.world_position / SavedBackboneGraph | viewer hit 点の z=0 をそのまま使い、中間分岐が地面から出る回帰防止 |
| C568 | Boundary | backbone | source-edge midair branch は current source curve projection を使う | backbone span 中間 pick + selected LV bundle | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: saved ownerless node は source identity と支持点高さを保持し、layout endpoint は source curve projection として導出する | SavedBackboneNode.source_edge / SpanLayoutEntry endpoint / SavedBackboneGraph.edges | source edge 上の branch が既存 main context を失う、支持点高さを接続点高さへ混ぜる、または endpoint が source curve から離れる回帰防止 |
| C569 | Boundary | backbone | backbone render cache は cable template appearance を使う | backbone LV line | GenerateFromBackboneSpec | Boundary: draw/render は topology を再判断せず、span bundle の cable template から radius/color/material だけを転写する | SpanRenderCacheEntry / CableTemplate | backbone の render cache が default radius/color のままになり、template 表示属性が初期生成に反映されない回帰防止 |
| C570 | Boundary | backbone | backbone lowering は SupportArm visual を出さない | backbone 3点 route + pass-through lowering | GenerateFromBackboneSpec | Boundary: lowered endpoint は positive down offsetを保持しつつsupport/endpointが一致し、SpanVisualCacheEntryにSupportArmがない | SpanVisualCacheEntry / VisualSettings | 旧SupportArmがvisual設定経由で復活する回帰防止 |
| C571 | Boundary | backbone | insulator visual disabled でも lowering は最終socketで解決する | insulator disabled + backbone 3点 route + pass-through lowering | UpdateVisualSettings→GenerateFromBackboneSpec | Boundary: rules/layout/geom/render は生成し、lowered endpoint は positive down offset を保持しつつ support/endpoint が一致する | SpanVisualCacheEntry / VisualSettings / SpanLayoutEntry | insulator visual 設定を無効化しても lowering/layout/geom を壊す、または旧placeholderを復活させる回帰防止 |
| C572 | Boundary | backbone | obsolete SupportArm設定はproduction surfaceへ存在しない | source scan | wire_core_tests | Boundary: core runtime settings、WASM bindings、Web model typeに旧support arm設定とkSupportArmが存在しない | VisualSettings / VisualPartKind / WASM bindings / Web model | dead compatibilityとして旧SupportArm familyを残す回帰防止 |
| C573 | Boundary | backbone | backbone saved context node は support metadata を保持する | saved graph context node を `local_for_saved` で graph に読む | GenerateFromBackboneSpec | Boundary: context node は saved graph の support kind / source-edge metadata を保持し、pole 前提の local node へ潰さない | SavedBackboneNode / backbone graph node | ownerless/building/ground context node が support/source metadata を失い、後続の context/placement 判断が pole 前提に痩せる回帰防止 |
| C574 | Boundary | backbone | backbone は existing edge に別 bundle を pass-through mode 付きで追加できる | A-B saved edge 後、same edge に Communication bundle + kPassThrough を追加 | GenerateFromBackboneSpec | Boundary: saved edge は共有し、node mode は pair/open を変えず layout/geom intent だけに影響する | SavedBackboneGraph / BundleNodeMode::kPassThrough / edge_bundle | same-edge bundle 追加で node mode が duplicate edge/span 判定や context 解釈を壊す回帰防止 |
| C575 | Boundary | backbone | backbone は stale segment-pick midair duplicate request を mutation 前に reject する | segment pick 由来 pending midair id で branch 生成後、same branch を stale source id + explicit saved target pole で再送 | GenerateFromBackboneSpec | Boundary: pending ownerless node は source saved edge + t で既存 saved node に解決し、duplicate policy が emit 前に効く | SavedBackboneGraph / SupportNode source_edge / duplicate preflight | stale pending id が新しい ownerless node と span を再生成し、duplicate policy をすり抜ける回帰防止 |
| C576 | Boundary | backbone | backbone ownerless-only multiple bundle route は pole type を要求しない | 両端 ownerless midair + LV/Communication + invalid pole_type | GenerateFromBackboneSpec | Boundary: new pole が無い route では pole type を解決せず、bundle/pair/row/layout/geom を生成する | ownerless support / pole type / multiple bundles | ownerless-only route が不要な pole type ambiguity に引きずられて unsupported になる回帰防止 |
| C577 | Boundary | backbone | backbone missing port band は topology mutation 前に reject する | LV pole band を削除して 2点 line 生成 | GenerateFromBackboneSpec | Boundary: port band preflight が AddPole/AddPort/AddBundle/AddSpan 前に失敗し、saved graph と generated object count を増やさない | PortPlacementBand / mutation preflight | missing band が emit_ports まで進んで partial topology を残す回帰防止 |
| C578 | Boundary | backbone | backbone segment pick midair branch は pass-through を受ける | A-B を生成後、segment pick 由来 pending midair node から B-D 相当 branch を生成し、同 point に kPassThrough を指定 | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: pending ownerless source-edge context を pass-through gate が受け、pair/open/row は変えず lowered layout/geom を生成する | SupportNode source edge context / BundleNodeMode::kPassThrough | segment pick 直後の ownerless branch が saved node 未作成という理由だけで pass-through unsupported になる回帰防止 |
| C579 | Boundary | backbone | backbone は polyline 上の単一 avoid detour を supported scenario として受ける | 3点 route の1 segment 上に `avoid_points` + `avoid_radius_m > 0` | GenerateFromBackboneSpec | Boundary: input route の該当 segment だけを upstream で deterministic detour へ変換し、他 segment は通常 route として生成する | BackboneGenerationConstraints / SavedBackboneGraph | avoid 対応が 2点 line に限定され、polyline 操作が不要に unsupported になる回帰防止 |
| C580 | Boundary | backbone | backbone は interval と positive avoid detour を segment order で生成する | `interval_m > 0` + route 上 `avoid_points` + `avoid_radius_m > 0` | GenerateFromBackboneSpec | Boundary: interval point と detour point は source segment 上の `t` 順に upstream route へ挿入し、downstream は route を再解釈しない | BackboneGenerationConstraints / SavedBackboneGraph | interval point と detour point の順序が逆転し、route が折り返す回帰防止 |
| C581 | Boundary | backbone | backbone は inactive bundle の missing port band を無視する | segment pick 由来 pending support で LV/HV を要求し、inactive HV band を削除 | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: port band preflight は materialize 対象の active bundle だけを検査し、pending policy で inactive になった bundle は生成判定に使わない | active_bundle_indices / PortPlacementBand / SupportNodeBundleMode | 生成しない bundle の band 不足で supported request が不自然に reject される回帰防止 |
| C582 | Boundary | backbone | backbone は同一 segment 上の複数 avoid point を supported scenario として受ける | 2点 route 上に2つの positive avoid point | GenerateFromBackboneSpec | Boundary: 複数 avoid point が同じ source segment にだけ交差する場合、detour point を `t` 順に upstream route へ挿入する | BackboneGenerationConstraints / SavedBackboneGraph | avoid point が複数あるだけで viewer 操作が不要に unsupported になる回帰防止 |
| C583 | Boundary | backbone | backbone は複数 segment 上の avoid point を supported scenario として受ける | 3点 route の2つの source segment 上に positive avoid point | GenerateFromBackboneSpec | Boundary: source segment ごとに detour point を `t` 順に upstream route へ挿入し、downstream は route を再解釈しない | BackboneGenerationConstraints / SavedBackboneGraph | polyline の複数 segment に avoid point があるだけで viewer 操作が不要に unsupported になる回帰防止 |
| C584 | Boundary | backbone | backbone は ownerless route の interval 挿入点を ownerless node として扱う | midair endpoint だけの route + `interval_m > 0` + no pole type | GenerateFromBackboneSpec | Boundary: source segment 両端が同じ ownerless support の場合、interval 挿入点も同じ support kind を継承し、pole type / pole band を要求しない | BackboneInputSpec.node_specs / SavedBackboneGraph | ownerless-only route の中間 interval point が暗黙 pole になり、不要な pole type 要求や pole 生成が起きる回帰防止 |
| C585 | Boundary | backbone | backbone は重複 avoid point を1つの detour node に畳む | 同じ座標の positive avoid point が2つある 2点 route | GenerateFromBackboneSpec | Boundary: 同じ source segment 上で同じ detour 位置になる avoid point は upstream route 挿入前に coalesce し、downstream に zero-length link を渡さない | BackboneGenerationConstraints / SavedBackboneGraph | duplicate UI payload が同一点 detour node を複数作り、zero-length link で supported 操作が失敗する回帰防止 |
| C586 | Boundary | backbone | backbone は interval point と avoid detour が同じ source 位置にある場合 detour を優先する | `interval_m` の挿入位置と positive avoid point が同じ 2点 route | GenerateFromBackboneSpec | Boundary: 同じ source segment 上で同じ `t` の interval point と avoid detour が競合した場合、obstacle 内の interval point を downstream に渡さず detour node だけを挿入する | BackboneGenerationConstraints / SavedBackboneGraph | interval point が obstacle 中心に残り、avoid detour を作っても route_clear で reject される回帰防止 |
| C587 | Boundary | backbone | backbone は selected bundle 未指定でも明示 midair node 作成を受ける | backbone saved span を segment pick し、`create_midair_node=true` かつ selected bundle 空 | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: 明示 create_midair_node は bundle policy が空でも pending ownerless midair node id を返し、後続 backbone generation は saved graph context だけで branch を生成する | PickResult / ResolveBranchPickOptions / SupportNode / SavedBackboneGraph | selected bundle 未指定の viewer 操作が invalid node id になり、後続 backbone node_specs へ渡せない回帰防止 |
| C588 | Boundary | backbone | backbone は internal corner 上の avoid point を deterministic detour として受ける | 3点 polyline の interior vertex に `avoid_points` + `avoid_radius_m > 0` | GenerateFromBackboneSpec | Boundary: endpoint avoid は一般 routing として残しつつ、内部 route vertex に重なる obstacle は upstream route point を deterministic detour に置き換え、downstream は route を再解釈しない | BackboneGenerationConstraints / SavedBackboneGraph / DetailCurve.sample_points | corner 上の obstacle が segment t=0/1 として detour 対象から外れ、route_clear で supported viewer 操作が止まる回帰防止 |
| C589 | Boundary | backbone | backbone は selected-bundle midair policy で未選択 bundle を生成しない | backbone span 中間 pick + selected Communication、後続 request は LV+Communication | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: pending support node が bundle policy を持つ場合、明示されていない bundle は materialize 対象から外し、selected bundle だけを生成する | SupportNode.bundle_modes / active_bundle_indices / generated bundle_ids | viewer の選択 bundle から作った midair branch が、未選択 bundle まで一緒に生成する回帰防止 |
| C590 | Boundary | backbone | backbone は selected policy で inactive になった bundle の pass-through を no-op 前に拒否する | selected Communication の pending midair node に LV request + LV kPassThrough | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: active bundle が空でも node mode intent を mutation 前に検査し、inactive bundle の pass-through は unsupported にする | SupportNode.bundle_modes / active_bundle_indices / BundleNodeMode | viewer の選択範囲で生成対象外になった bundle の node mode が黙って無視される回帰防止 |
| C591 | Boundary | backbone | backbone は saved selected midair node の継続生成で request bundle を使う | selected Communication の segment pick から midair branch を生成後、その saved midair node から LV+Communication で延長 | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: pending ownerless node の `bundle_modes` は最初の生成だけに使い、SavedBackboneNode には持ち越さない | SavedBackboneNode.bundle_modes / active_bundle_indices / generated bundle_ids | 中間分岐の selected policy が保存後の continuation まで絞り、テンプレート指定どおりの延長を妨げる回帰防止 |
| C592 | Boundary | backbone | backbone は saved selected midair node の reverse continuation でも request bundle を使う | C591 と同じ saved midair node に逆向き route endpoint として接続 | GenerateFromBackboneSpec | Boundary: saved midair node は route 方向に依存せず保存済み selected policy を再適用しない | SavedBackboneNode.bundle_modes / PathDirection | reverse continuation だけ saved policy が残り、テンプレート指定どおりの延長を妨げる回帰防止 |
| C593 | Boundary | backbone | backbone は saved selected midair node で request pass-through を受ける | selected Communication の saved midair node に LV request + LV kPassThrough | GenerateFromBackboneSpec | Boundary: saved ownerless node は過去の selected policy で request の node mode を inactive 扱いしない | SavedBackboneNode.bundle_modes / BundleNodeMode | saved node 化後の古い selected policy が明示 request を拒否する回帰防止 |
| C594 | Boundary | backbone | backbone は route endpoint と完全一致する avoid point を no-op として扱う | 2点 route の start point と同じ座標の positive avoid point | GenerateFromBackboneSpec | Boundary: endpoint 完全一致の avoid point は固定 route endpoint を動かさず、detour node も作らず、endpoint 近傍一般回避には広げない | BackboneGenerationConstraints / SavedBackboneGraph | endpoint と同一点の UI payload が route_clear で不要に unsupported になる回帰防止 |
| C595 | Boundary | backbone | backbone は明示 existing support と完全一致する avoid point を no-op として扱う | 既存 pole を interior `node_specs` に指定し、その点と同じ avoid point を渡す | GenerateFromBackboneSpec | Boundary: 明示 existing support は動かさず、avoid point 完全一致だけを no-op とし、support 位置近傍一般回避には広げない | BackboneGenerationConstraints / BackboneInputSpec.node_specs / SavedBackboneGraph | UI payload が support 自体を avoid point に含めた時に、既存 support を detour したり unsupported へ落ちる回帰防止 |
| C596 | Boundary | backbone | backbone は明示 new support と完全一致する avoid point を no-op として扱う | 明示 new pole `node_specs` と同じ座標の positive avoid point | GenerateFromBackboneSpec | Boundary: 明示 new support は動かさず、avoid point 完全一致だけを no-op とし、support 位置近傍一般回避には広げない | BackboneGenerationConstraints / BackboneInputSpec.node_specs / SavedBackboneGraph | clicked support と同じ avoid payload で明示 support が detour されたり unsupported へ落ちる回帰防止 |
| C597 | Boundary | backbone | backbone は selected building pick で選択 bundle だけを生成する | `PickHitKind::kExternal` + selected Communication、後続 request は LV+Communication | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: building pick の selected bundle policy は ownerless route point の materialize 対象選択だけに使い、未選択 bundle は生成しない | ResolveBranchPickResult / SupportNode.bundle_modes / active_bundle_indices | viewer の選択 bundle から building route point を作った時に、未選択 bundle まで一緒に生成する回帰防止 |
| C598 | Boundary | backbone | backbone は selected saved building node pick で選択 bundle だけを生成する | saved building node を `PickHitKind::kNode` で選択し、selected Communication のまま LV+Communication branch を生成 | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: node pick でも selected bundle policy は ownerless route point の materialize 対象選択だけに使い、未選択 bundle は生成しない | ResolveBranchPickResult / SupportNode.bundle_modes / active_bundle_indices | viewer の node pick 経由だけ selected bundle policy が失われ、未選択 bundle まで生成する回帰防止 |
| C599 | Boundary | backbone | backbone は selected saved building node の bundle policy を branch 後も保持する | saved building node を selected Communication で branch した後、同じ saved node から LV+Communication continuation を生成 | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: selected policy は pending branch だけで消えず SavedBackboneNode に保存され、後続 continuation でも未選択 bundle を生成しない | SavedBackboneNode.bundle_modes / active_bundle_indices | selected saved node の初回 branch だけ選択 bundle になり、後続 continuation で未選択 bundle が復活する回帰防止 |
| C600 | Boundary | backbone | backbone は selected existing pole pick で選択 bundle だけを生成する | saved pole node を `PickHitKind::kNode` で選択し、selected Communication のまま LV+Communication branch を生成 | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: pole pick の selected bundle policy は transient pending pole node として現在の materialize 対象選択だけに使い、saved pole node へ永続化しない | ResolveBranchPickResult / pending SupportNode / active_bundle_indices / SavedBackboneNode.bundle_modes | existing pole pick だけ selected bundle policy が失われ、未選択 bundle まで生成する回帰防止 |
| C601 | Boundary | backbone | backbone は context-only node の selected policy で新規 route bundle を絞らない | selected Communication の segment-pick midair branch を作った後、その endpoint pole から LV+Communication continuation を生成 | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: context-only saved node の bundle policy は pair/open/row context には使えても、新規 materialized route の active bundle 選択には使わない | SavedBackboneGraph context link / SavedBackboneNode.bundle_modes / active_bundle_indices | context 入力だけの selected policy が後続 route の bundle 生成を誤って削る回帰防止 |
| C602 | Boundary | backbone | saved band を持たない pole type 適用は mutation 前に拒否し、無関係 route は影響を受けない | A-B 生成後、LV band のない pole type を B へ適用し、A-C の LV branch を生成 | ApplyPoleType→GenerateFromBackboneSpec | Boundary: B の pole type 適用は unsupported で state 不変、A-C は通常生成できる | SavedBackbonePortBinding / PortPlacementBand / SavedBackboneGraph context | binding band 不足を fallback で補完する回帰と、拒否が無関係 route を汚す回帰を防止 |
| C603 | Boundary | backbone | backbone は context node で generated endpoint pole yaw を曲げない | A-B 生成後、A-C branch を追加し、context B が graph に存在する状態で C pole yaw を確認 | GenerateFromBackboneSpec | Boundary: generated pole yaw は materialized route 隣接点から決め、context-only node の vector を使わない | Pole.world_transform.rotation_euler_deg / SavedBackboneGraph context node | context node が route node の次要素として扱われ、新規 endpoint pole の向きが branch 方向から外れる回帰防止 |
| C604 | Boundary | backbone | backbone は大きい avoid 半径でも deterministic detour を半径外へ出す | 2点 line の中央に `avoid_radius_m=4` の avoid point | GenerateFromBackboneSpec | Boundary: avoid detour offset は detour 後の隣接 segment が半径を避ける距離で決め、endpoint へ半径が届く場合は unsupported | BackboneGenerationConstraints / generated pole positions | detour point 自体は動いたが折れ線 segment がまだ obstacle 半径内を通り、後段 preflight で supported 入力が reject される回帰防止 |
| C605 | Boundary | backbone | backbone の route query は saved ownerless backbone graph を読む | midair node を含む backbone route を生成し、saved midair node から terminal pole まで FindSavedBackboneRoute | FindSavedBackboneRoute | Boundary: saved graph がある場合は span-derived edge ではなく SavedBackboneGraph node/edge/edge_bundle から route を説明する | SavedBackboneGraph / BackboneIndex / FindSavedBackboneRoute | ownerless endpoint が pole id を持たないため、span-derived SavedBackboneEdges だけでは public route query が空になる回帰防止 |
| C608 | Boundary | backbone | SavedBackboneResult は saved pole node を重複表示しない | 2点 backbone route 後に SavedBackboneResult を読む | GenerateFromBackboneSpec→SavedBackboneResult | Boundary: SavedBackboneGraph がある場合、public backbone query は span-derived pole node を混ぜず saved graph node を正とする | SavedBackboneGraph / BackboneResult.nodes | saved graph node と materialized pole id node が二重計上され、public query の node 数・junction 判断が膨らむ回帰防止 |
| C607 | Boundary | backbone | SavedBackboneResult は saved ownerless node の今回 route index を保つ | 明示 midair 中点を生成後、その saved midair から追加 route を生成して SavedBackboneResult を読む | GenerateFromBackboneSpec→SavedBackboneResult | Boundary: saved graph node を public query に投影しても current route の `path_point_index` を落とさない | SavedBackboneGraph / BackboneResult.nodes.path_point_index | saved graph 正本を優先した結果、debug route metadata が上書きされ、明示 non-pole support を public query で再特定できなくなる回帰防止 |
| C606 | Boundary | backbone | SavedBackboneResult は saved ownerless node を説明する | 明示 midair 中点を含む backbone route 後に SavedBackboneResult を読む | GenerateFromBackboneSpec→SavedBackboneResult | Boundary: public backbone query は span/layout/seed 推測ではなく saved graph node を説明できる | SavedBackboneGraph / BackboneResult.nodes | backbone saved ownerless topology が public query から見えず旧観測 API 経由の実用確認ができない回帰防止 |
| C609 | Boundary | backbone | 通常の bend は lowering intent を作らない | 3点 polyline + `enable_branch_down_offset` を持つ bundle | GenerateFromBackboneSpec | Boundary: bend の全 endpoint で `support_world == endpoint_world == port.world_position` | SpanLayoutEntry / Port | route bend を branch/cross conflict と誤認して lowering する回帰防止 |
| C610 | Boundary | backbone | row conflict は許可された bundle の junction endpoint だけを offset する | LV/HV/Communication/Optical を持つ3点 routeへ junction branch追加 | GenerateFromBackboneSpec | Boundary: template policyを持つHV laneだけjunction側endpointがsigned offsetされ、他bundleと反対endpointは不変 | BundleTemplate / SpanLayoutEntry | pair idをrow idとして扱う、またはmulti-row nodeの全bundle/全endpointを下げる回帰防止 |
| C611 | Boundary | backbone | backbone direct derive は saved rule から span outputs を復元する | 2点 backbone route 生成後に layout/curve/bounds/visual/render cache を消す | GenerateFromBackboneSpec→DeriveGeneratedSpanOutputs | Boundary: saved `SpanLayoutRule` から `SpanLayoutEntry` / `DetailCurve` / `BoundsCacheEntry` / draw caches を再保存し、追加 input を要求しない | SpanLayoutRulesView / SpanLayoutView / CurveCacheEntry / BoundsCacheEntry / SpanVisualCacheEntry / SpanRenderCacheEntry | post-edit/rederive が `Commit(run_recalc)` や support-layout materialization に戻る回帰防止 |
| C612 | Boundary | backbone | backbone direct derive は recalc/materialization 経路を呼ばない | source scan | wire_core_tests backbone | Boundary: `core/src/generation/backbone/derive.cpp` は `Commit` / `ProcessDirtyQueues` / `cache_span_support_layout` / `support_layout_projection` / `materialization` を呼ばず direct cache 保存だけを行う | source text | direct derive entrypoint が名前だけで内部は recalc wrapper になる回帰防止 |
| C613 | Boundary | backbone | backbone port edit は Commit なしで generated span outputs を再導出する | 2点 backbone route 生成後、generated span endpoint port を手動移動 | GenerateFromBackboneSpec→SetPortWorldPositionManual | Boundary: saved graph binding + saved rule を持つ span は layout/curve/bounds/draw cache が即時更新される | SavedBackboneGraph / SpanLayoutRule / SpanLayoutEntry | viewer normal path が port edit 後の表示更新を `Commit(run_recalc)` に依存する回帰防止 |
| C614 | Boundary | backbone | backbone update plan は4分類だけを使う | 2点 backbone route 後に pole/port/all/span target で plan 作成 | CoreStateTestHook::make_update_plan | Boundary: pole/port は kReposition、all spans は kReshape/kRedraw、topology系は kRegenerate として分類される | UpdateKind / UpdatePlan / SavedBackboneGraph bindings | setterやderive側が操作名別 dirty enum を増やす回帰防止 |
| C615 | Boundary | backbone | kRegenerate は local fallback 実行しない | 2点 backbone route の span target で kRegenerate plan 実行 | make_update_plan→execute_update_plan | Boundary: local executor は kRegenerate を unsupported にし、暗黙に部分再生成しない | UpdatePlan | route/island regeneration 仕様なしで fallback 実装する回帰防止 |
| C616 | Boundary | backbone | kReposition は SavedBackboneGraph identity を変えない | 2点 backbone route 後に endpoint port を移動 | SetPortWorldPositionManual | Boundary: saved nodes/edges/edge_bundles/span_bindings の id は変わらず、出力だけ更新される | SavedBackboneGraph / SpanLayoutEntry | port/pole 移動が topology 再決定や graph 再保存に戻る回帰防止 |
| C617 | Boundary | backbone | kReshape は layout を書き換えない | 2点 backbone route 後に sag 系 GeometrySettings を変更 | UpdateGeometrySettings | Boundary: layout endpoint/source_version は保持し、geom/draw 側だけ再導出する | SpanLayoutEntry / GeometrySettings | sag 変更で placement/layout を再判定する回帰防止 |
| C618 | Boundary | backbone | kRedraw は layout/geom を書き換えない | 2点 backbone route 後に VisualSettings を変更 | UpdateVisualSettings | Boundary: layout/curve/bounds は保持し、visual/render だけ更新する | SpanLayoutEntry / DetailCurve / BoundsCacheEntry / VisualSettings | 表示設定変更で geometry や placement を再導出する回帰防止 |
| C619 | Boundary | backbone | kReposition は affected span だけ更新する | 3点 backbone route 後に端点 port を移動 | SetPortWorldPositionManual | Boundary: moved port に接続する span だけ layout が更新され、無関係 span の layout source は変わらない | SavedBackboneGraph bindings / SpanLayoutEntry | affected set が全 span 更新に戻る回帰防止 |
| C620 | Boundary | backbone | update boundary に操作別 kind を作らない | source scan | wire_core_tests backbone | Boundary: `UpdateKind` は kRegenerate/kReposition/kReshape/kRedraw に限定し、derive は recalc/materialization/support-layout contract を呼ばない | source text | PoleTilt/PoleYaw/Sag 等の細粒度 enum が増殖する回帰防止 |
| C621 | Boundary | backbone | sag は geom/draw だけを reshape する | 2点 backbone route + sag enabled | UpdateGeometrySettings | Boundary: layout endpoint と SavedBackboneGraph identity は不変のまま DetailCurve に sag が付き bounds が追従する | SpanLayoutEntry / DetailCurve / BoundsCacheEntry / SavedBackboneGraph | backbone wire が直線固定へ戻る、または sag が topology/layout を再決定する回帰防止 |
| C622 | Boundary | backbone | pipeline/update timing は診断専用 | 3点 backbone route + reshape update | GenerateFromBackboneSpec / UpdateGeometrySettings | Boundary: generation stage と update plan/derive の timing が非負で、UpdateKind と affected span 数に追従する | GenerationTiming / UpdateTiming | global profiler や timing 起点の生成分岐を作らず性能観測の入口を固定する |
| C623 | Boundary | backbone | backbone layout settings は regenerate で派生出力を更新する | 3点route生成後に LayoutSettings 変更 | UpdateLayoutSettings | Boundary: 更新は成功し、layout settings が変わり、change_set に更新対象が入る | LayoutSettings / ChangeSet | layout settings を stale 成功または全拒否へ戻す回帰防止 |
| C624 | Boundary | backbone | variation settings は stale 成功しない | generated span + variation change | UpdateVariationSettings | unsupported かつ設定不変 | VariationSettings | 未接続設定の成功扱い防止 |
| C625 | Boundary | backbone | context profile は stale 成功しない | generated span + context change | UpdateContextProfile | unsupported かつ設定不変 | ContextProfile | 未接続profileの成功扱い防止 |
| C626 | Boundary | backbone | cable shape/render 更新は direct derive | generated span + sag/color change | UpdateCableTemplate | sag は geom、color は renderへ即時反映しlayout不変 | curve/render/layout | dirty markerなしで出力更新を保証 |
| C627 | Boundary | backbone | 旧 topology mutation API は public surface に残さない | public header / CMake source scan | wire_core_tests | AddConnection/AddDrop/SplitSpan と専用 result/options 型が存在しない | CoreState / core_state_api_types / CMake | 旧 API が graph 外 topology 経路として復活する回帰防止 |
| C628 | Boundary | backbone | active backbone pole type は placement-only なら再導出する | generated line の pole type placement を変更 | GenerateFromBackboneSpec→UpdatePoleTypeDefinition | placement-only は port/layout/geom/draw を更新し SavedBackboneGraph identity は不変 | pole type / generated outputs / SavedBackboneGraph | Pole Placement UI を既存線へ反映する回帰防止。構造差分は C713 が統一 regenerate と fresh 等価性で固定する |
| C629 | Invariant | geometry | parabolic main-span curve の意味入力を固定する | horizontal span + sag | BuildCableCurve | endpoint exact、midpoint sag exact、top-view lateral driftなし | CableCurveOutput / DetailCurve adapter | caller側Bezier handleや横揺れをauthorityにする回帰防止 |
| C630 | Invariant | geometry | curve sample frame はfiniteかつorthonormal | sloped span + sag | BuildCableCurve | tangent/normal/binormalがfinite、unit、相互直交 | CableCurveSample | Frenet由来のnear-straight twistやNaN防止 |
| C631 | Invariant | geometry | reverse traversalでもcanonical lateral frameを維持する | 同一edgeをforward/reverseで生成 | BuildCableCurve | 対応sampleのposition/binormalが一致 | CableCurveSample | save/loadやedge traversal反転でlane左右が反転する回帰防止 |
| C632 | Invariant | geometry | tessellationはlength/sagに応じる | short/long/high-sag input | ResolveSegmentCount | longまたはhigh-sagでsegment数増加、policy範囲内 | TessellationPolicy | caller固定sample countへの逆戻り防止 |
| C633 | Invariant | geometry | zero-length/vertical curveを決定的に処理する | degenerate/vertical/unknown method | BuildCableCurve | finite deterministic frame、unknown methodは明示reject | CableCurveOutput / EditResult | near-world-up fallback不定・silent method fallback防止 |
| C634 | Boundary | backbone | terminal node は NodePatchCurve を作らない | 2点 backbone route | GenerateFromBackboneSpec | Boundary: terminal endpoint は接続部曲線を持たず EdgeBodyCurve が直接 endpoint に接続する | VisualCurvePartCache / VisualCurvePartKind | terminal まで span-local attachment blend を作る回帰防止 |
| C760 | Invariant | backbone | terminal EdgeBody は resolved port endpoint に直接接続する | 2点 backbone route | GenerateFromBackboneSpec | base EdgeBody の両 boundary が Span 両端 Port の world position と一致する | VisualCurvePartCache / Port | patch無しを理由に terminal wire が port から浮く回帰を防ぐ |
| C761 | Invariant | default registry | default OPTICAL bundle はmember endpoint追従のsupportと6 samples/turnのhelixを出力し、COMMは通常線のままにする | OPTICAL 2点 route + default COMM/OPTICAL template、OPT band高さ+1m | GenerateFromBackboneSpec→UpdatePoleTypeDefinition | OPTICAL logical span ごとにsupport pathとhelixが各1本。band 0により全body/support/helix sampleが高さ変更へ同量追従する | BundleTemplate / VisualCurvePartCache | Helixの既定対象がCOMMへ戻る、固定高support bandで断面が肥大化する、端部だけ動いて懸垂部が残る、またはsamplingが過剰になる回帰を防ぐ |
| C635 | Boundary | backbone | simple 2-edge continuous node は NodePatchCurve を作る | 3点 backbone route | GenerateFromBackboneSpec | Boundary: shared connection patch は span ではなく node/bundle/lane 単位の派生 part として一度だけ作る | VisualCurvePartCache / NodePatchClassification | 前後 span がそれぞれ同じ junction 接続部を丸める回帰防止 |
| C636 | Boundary | backbone | EdgeBodyCurve は NodePatch 境界で止まる | 3点 backbone route | GenerateFromBackboneSpec | Boundary: patch がある node では EdgeBodyCurve endpoint は raw node center ではなく patch boundary を使う | VisualCurvePart boundary / SavedBackboneNode | edge body が node patch 内まで食い込み、接続部 ownership が混ざる回帰防止 |
| C637 | Boundary | backbone | NodePatchCurve と EdgeBodyCurve は元の懸垂曲線へG1接続する | sag付き3点 backbone route | GenerateFromBackboneSpec | Boundary: EdgeBodyの全sampleは元span curve上のtrimmed区間にあり、patch boundaryの接線をNodePatchと共有する | CurveCacheEntry / VisualCurvePart samples | trim後bodyをHermiteで再構築して端部へ曲率を集中させる、またはpatchとbodyだけを同じ誤接線へ揃える回帰防止 |
| C638 | Boundary | backbone | visual curve part samples は finite | 3点 backbone route | GenerateFromBackboneSpec | Boundary: derived visual curve part は topology authority ではないが、debug/render 可能な finite samples と bounds を持つ | VisualCurvePart samples / bounds | NodePatch/EdgeBody 導入で NaN/inf や空 debug geometry を保存する回帰防止 |
| C639 | Boundary | backbone | NodePatchCurve は直線 chord ではない | 3点 backbone route | GenerateFromBackboneSpec | Boundary: simple continuous patch は折れ線や straight chord ではなく local curve samples を持つ | VisualCurvePart samples / boundary | 接続部の丸みが消え、patch が debug 名だけの折れ線に戻る回帰防止 |
| C640 | Boundary | backbone | NodePatchCurve の Bezier debug controls を観測できる | 3点 backbone route | GenerateFromBackboneSpec | Boundary: Bezier 制御点は topology 正本ではなく visual debug 用の派生情報として観測可能にする | VisualCurvePart bezier_control_points | 見た目違和感の調査で viewer 側が推測や補正を始める回帰防止 |
| C641 | Boundary | backbone | Pole tilt 後も visual curve parts が所有 endpoint に追従する | 2点 backbone route + generated pole | ApplyPoleTilt | Boundary: layout endpoint と EdgeBodyCurve boundary が同時に移動して一致する | span_layout / visual_curve_parts | direct derive 後に古い curve part cache を残さない |
| C659 | Boundary | backbone | draw-time tilt は port/span 生成前に support frame へ入る | 2点 backbone route + `pole_placement.enable_tilt` | GenerateFromBackboneSpec | Boundary: generated pole は Apply なしで tilt し、layout endpoint は tilted port world position と一致し、ApplyPoleTilt と同じ解決結果になる | BackbonePolePlacementOptions / Pole / Port / SpanLayoutEntry | DrawPath 後の final Apply API 依存や、見た目だけ tilt して endpoint が untilted のまま残る回帰防止 |
| C642 | Boundary | backbone | EdgeBodyCurve は正式 sag curve を共有する | 30m 2点 route + sag | GenerateFromBackboneSpec | Boundary: support tangent は非水平で、sample列は formal curve と一致し、長さに応じて8点を超える | curve_cache / visual_curve_parts | quartic sag 二重実装と固定8 sampleへの回帰防止 |
| C643 | Boundary | backbone | NodePatchCurve はattachment参照を保持した内側fillet | 3点 route | GenerateFromBackboneSpec | Boundary: attachmentはmiddle pole所有portとして保持するがmain cable sampleは通過しない | ports / visual_curve_parts | attachment通過制約による外振りへの回帰防止 |
| C644 | Boundary | backbone | patch boundary は main sag実接線の延長上でattachmentより下がる | 3点 route + sag | GenerateFromBackboneSpec | Boundary: attachmentは固定し、両boundaryは外向き実接線方向へ水平距離ぶん延長される | visual_curve_parts | attachmentと同高の水平patchや固定down offsetへの回帰防止 |
| C645 | Boundary | backbone | NodePatch内側filletは局所曲率を維持する | 3点 route + sag | GenerateFromBackboneSpec | Boundary: 1区間cubicの両controlはboundary chordから離れる | visual_curve_parts controls | filletがstraight chordへ退化する回帰防止 |
| C646 | Boundary | backbone | NodePatchはturn内側で単調に曲がる | 3点 route + sag | GenerateFromBackboneSpec | Boundary: sampleはboundary-attachment三角形内に留まり、接線回転符号が反転しない | visual_curve_parts samples | 左折前に右へ外振りする回帰防止 |
| C647 | Boundary | backbone | NodePatchはincident cableのappearanceを使う | 3点 route | GenerateFromBackboneSpec | Boundary: node-owned patchは架空のspan ownershipを持たず、接続するEdgeBodyと半径・色・material styleが一致する | visual_curve_parts appearance | source_span_idを持たないpatchだけviewer既定の太さ・色へ落ちる回帰防止 |
| C655 | Boundary | backbone | NodePatch grouping は band identity を含み、pair選択はbundle instanceを混ぜない | source scan | wire_core_tests backbone | Boundary: node/template/lane/sectionだけでなくpole typeとband idをcontinuity keyに含め、candidate pairは同Bundle、同Port共有、同一endpoint点を優先し、それが無いterminal continuationだけsaved pairへfallbackする | curve_patch_key / same_key / candidate selection | 同templateの別Bundle、上下band、別placement slotを同じ接続部としてpatch化する回帰防止 |
| C656 | Boundary | backbone | NodePatch は base section と extra section を混ぜない | population section enabled + 3点 route | GenerateFromBackboneSpec | Boundary: 1つのNodePatchが接続するEdgeBodyはbase/extra familyを跨がない | VisualCurvePart.section_key | base線とpopulation線がjunctionで接続される回帰防止 |
| C657 | Boundary | backbone | extra section は同一 instance だけを接続する | 2 extra sections + 3点 route | GenerateFromBackboneSpec | Boundary: saved pair rowで隣接sectionを接続し、同一patchに異なるinstance_indexを混ぜない | SavedBackboneRowKey / CableSectionKey.instance_index | extra instance 0と1がjunctionで交差接続される、または同じinstanceがspan境界で切れる回帰防止 |
| C648 | Invariant | backbone population | 同じexplicit seedは同じspan section populationを返す | pure population input | populate_cable_sections | Invariant: requested count、section key、両endpoint位置が一致し、横位置と高さがrule範囲内に入る | CablePopulationOutput | redrawで追加線が並べ替わる回帰防止 |
| C649 | Invariant | backbone population | logical span identityだけではpopulation配置seedを変えない | span idだけ異なるpure input | populate_cable_sections | Invariant: section endpoint位置は同一になる | CableSectionLayout | 同じ生成Bundle上の追加線が電柱間ごとに別位置へ飛び、junctionで切れる回帰防止 |
| C650 | Invariant | backbone population | reserveはcandidate pairを拒否する | band全体を覆うreserve | populate_cable_sections | Invariant: accepted=0、requested分をomit | population diagnostic | reserved領域へ線を置く回帰防止 |
| C651 | Invariant | backbone population | spacing不足はcandidateを拒否する | 狭いband + 大きいspacing | populate_cable_sections | Invariant: overlap candidateをacceptしない | CableSectionLayout | 密集線が同位置へ重なる回帰防止 |
| C652 | Boundary | backbone population | 片endpoint失敗はsection全体をomitする | endpoint B unavailable | populate_cable_sections | Boundary: 片側だけ生成せず全requested sectionをomit | population diagnostic | endpointを独立solveして線がねじれる回帰防止 |
| C653 | Boundary | backbone population | band identity重複を拒否する | 同じPoleType内にduplicate band_id | has_duplicate_band_ids | Boundary: duplicateを検出する | PortPlacementBand | vector順で曖昧bandを選ぶ回帰防止 |
| C654 | Boundary | backbone population | span section populationはlogical topologyを変更しない | control/population fresh state | GenerateFromBackboneSpec | Boundary: Pole/Port/Bundle/Span/SavedGraph counts一致、base/extraが同じEdgeBody visual経路を使う | CoreView / VisualCurvePartCache | 見た目用追加線がtopologyまたは別curve familyになる回帰防止 |
| C658 | Boundary | geometry | CableCurve は endpoint tangent hints を実サンプルに使う | explicit tangent hints + sag | BuildCableCurve | Boundary: start/end sample tangent が指定hintと一致し、hint無しの既存parabolic pathとは分かれる | CableCurveInput / CableCurveOutput | NodePatch境界tangentをEdgeBodyがmetadataだけ持ち、描画samplesでは無視する回帰防止 |
| C688 | Invariant | geometry | 片側hintのCableCurveはhint無し端の自然なsag端点接線を保つ | 片側start hint + sag 付き水平span | BuildCableCurve | Invariant: hint無し端のsample tangentはhint無し生成の端点接線と一致(弦方向へ寝ない)。hintが自然接線と等しい場合、全sampleがhint無しparabolic pathと一致する | CableCurveOutput.samples | 3点以上routeの終端spanだけ弦方向接線+端点微分ゼロの装飾sagへ切り替わる回帰防止 |
| C730 | Invariant | backbone/template | BundleKind は BundleTemplate identity ではない | 同じ LowVoltage kind の BundleTemplate 2件 | CoreStateTestHook::bundle_templates | Invariant: 2件が別 BundleTemplateId で同時に保存され、kind は重複できる | CoreView.bundle_templates | BundleKind keyed map へ戻る回帰防止 |
| C731 | Invariant | backbone/template | BackboneSpec は同 kind 別 template を区別する | 同じ LowVoltage kind の2 templateを1 routeへ指定 | GenerateFromBackboneSpec | Invariant: 生成 Bundle がそれぞれ別 BundleTemplateId を保持する | Bundle.bundle_template_id | BackboneBundleSpec が BundleKind identity に戻る回帰防止 |
| C732 | Invariant | backbone/population | population rule owner は BundleTemplateId | 同 kind template A/B のうち A のみに population rule | GenerateFromBackboneSpec | Invariant: extra section の rule_owner_id は A id だけで、B id には出ない | VisualCurvePart.section_key.rule_owner_id | BundleKind 単位で population rule が混線する回帰防止 |
| C733 | Invariant | backbone/update | regenerate scope は BundleTemplateId | 同 kind template A/B 生成後に A の count を変更 | UpdateBundleTemplate | Invariant: A bundle だけ conductor_count が変わり B は維持される | Bundle.conductor_count / bundle_template_id | regenerate scope が kind で集まる回帰防止 |
| C734 | Invariant | backbone/render | cable template lookup は kind based ではない | 同 kind template A/B に別 CableTemplateId を設定 | GenerateFromBackboneSpec | Invariant: B の visual part だけ B cable color を使い、A は混線しない | VisualCurvePart.color_rgba / bundle_template_id | CableTemplate lookup が BundleKind に戻る回帰防止 |
| C735 | Boundary | backbone/template | source guard は BundleKind identity 残存を拒否する | identity 重点ファイルの文字列検査 | wire_core_tests | Boundary: BundleKind keyed map / BundleKind bundle_template_id / kind owner 代入が無い | source files | 永続化前に enum identity を焼き戻す回帰防止 |
| C736 | Boundary | backbone/update | unsupported保留docsは対応済みbackbone更新を戻さない | docs source scan | wire_core_tests | Boundary: viewer_operations の stale unsupported 文言がなく、merge_readiness に UpdateLayoutSettings / span override / range化が保留行として戻らない | docs/merge_readiness.md / docs/viewer_operations.md | docsだけが古い unsupported を再導入し、viewer後追い作業を誤誘導する回帰防止 |
| C737 | Boundary | backbone/input | overlay edge endpoint snap は pole node spec id を返す | 中間分岐後、overlay edge の pole endpoint 近傍 pick | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: saved edge endpoint の saved node id を pole id として誤返却せず、返った node spec で延長生成できる | ResolveBranchPickResult / BackboneInputSpec.node_specs | web overlay edge endpoint から伸ばす操作が `node spec pole not found` で止まる回帰防止 |
| C691 | Invariant | backbone | run id は through section を接続する | 3点 route | GenerateFromBackboneSpec | Invariant: 同一laneのbase EdgeBody 2本とNodePatchが同じ非0 cable_run_idを持つ | VisualCurvePart.cable_run_id | section scope idのままspan境界で見た目上1本のcableが分断される回帰防止 |
| C692 | Invariant | backbone | run id は世代跨ぎterminal extensionを接続する | 2点 route 後に terminal pole から延長 | GenerateFromBackboneSpec x2 | Invariant: 旧spanと新spanのbase EdgeBodyが同じ非0 cable_run_idを持つ | VisualCurvePart.cable_run_id | 別生成回の連続sectionが別run扱いになる回帰防止 |
| C693 | Invariant | backbone | branchとdead-endは別runになる | T字 branch fixture + C663鋭角fixture | GenerateFromBackboneSpec | Invariant: branch span runはthrough runと異なり、鋭角dead-end側EdgeBodyは互いに異なる非0 runを持つ | VisualCurvePart.cable_run_id / Jumper | jumperやbranchをpatch連続として誤集約する回帰防止 |
| C694 | Invariant | backbone population | population instance ごとに run を接続する | 2 extra section + 3点 route | GenerateFromBackboneSpec | Invariant: extra instance k の両span EdgeBodyは同じrun、base/他instanceとは異なる | VisualCurvePart.section_key / cable_run_id | baseとpopulationまたは別instanceを同じrunへ混ぜる回帰防止 |
| C695 | Invariant | backbone | identityを持つcable sectionのrun idは決定的 | 同一入力 fresh state 2つ | GenerateFromBackboneSpec | Invariant: supplementalを除くvisual partごとのrun id snapshotが一致する | VisualCurvePart.cable_run_id | identityなしsupport/helixをrunへ偽装せず、pointer/order依存の不安定run id回帰を防止 |
| C696 | Boundary | backbone | run idはvisual derive層の派生値で、section indexから決定する | source scan | wire_core_tests | Boundary: run idはcore_runtime_typesとcurve_parts deriveに閉じ、CableSectionKey lookupはhash indexを使い、SavedBackboneGraph/save_backboneへ保存しない | source text | run identityをtopology正本へ保存する、またはsectionごとの線形探索へ戻る回帰防止 |
| C660 | Boundary | backbone | regenerate fixed count increase は SavedGraph identity を維持して下流だけ更新する | 2点 route + LowVoltage fixed count 1→2 | GenerateFromBackboneSpec→UpdateBundleTemplate | Boundary: node/edge/edge_bundle id は不変、span binding と rules/layout/geom/draw は count に合わせて更新 | SavedBackboneGraph / SpanLayoutEntry / curve / draw | route-local regenerate が入力replayや位置推測でgraphを作り直す回帰防止 |
| C661 | Invariant | backbone | pair row axis は径間長ではなく単位接線二等分で決める | 非対称な3点 corner + 2 lane bundle | GenerateFromBackboneSpec | Invariant: corner pole の2 lane方向は incoming/outgoing unit tangent sum の直交方向に一致し、旧chord直交方向とは一致しない | Port.world_position | pass-through cornerの横並びが長い径間へ引っ張られる回帰防止 |
| C662 | Invariant | backbone | pair row axis は incident span の lane順を反転しない | 非対称な3点 corner + 2 lane bundle | GenerateFromBackboneSpec | Invariant: row axis は incoming/outgoing それぞれのlink直交方向と正のdotを持つ | Port.world_position | row axis変更で片側spanのlane順がねじれる回帰防止 |

## Retired old-pipeline checks
- Old cases 365-367 were removed from the registered suite. They pinned the transitional `BackboneBuilder` / support-layout authority seed / materialization surface instead of the backbone mainline.
- Their constraints are now covered in backbone terms by the saved graph, pair/open/row authority, no-recalc/no-materialization, and layout/geom/draw consumer-chain checks.

## LLM self-review
- 実装依存か: private順序/内部関数呼び出し順には依存しない。
- 期待値は観測可能か: すべて公開APIと公開状態で観測。
- モック過多か: モック未使用。
- 異常系が入っているか: C10/C20/C21/C22/C23/C49で失敗診断・状態保全・復帰を検証。
- フレーク要因がないか: 実時間待ち/非決定乱数なし。
| C663 | Invariant | backbone | 鋭角 continuity は共有 pair row ではなく径間別 dead-end row と jumper で表す | 60度 corner + 2 lane bundle | GenerateFromBackboneSpec | Invariant: corner は4 port/2 open row、各 row は自 edge 直交、laneごとに jumper、NodePatchなし、SavedGraph edge/span数は不変 | SavedBackboneGraph / SpanLayoutRule / VisualCurvePart | 鋭角で横間隔を倍率補正し続ける、lane twist、または線が途切れる回帰防止 |
| C664 | Boundary | backbone | 鋭角pole facingはpairsのcorner decisionを消費し再判定しない | 60度 corner | GenerateFromBackboneSpec + source guard | Boundary: pole yawはjumper relationのnode_forwardと一致し、emit_polesに角度閾値/acosが無い | Pole.world_transform / pipeline source | pole yawとrow/jumperが別々に鋭角を解釈する回帰防止 |
| C665 | Boundary | backbone | source-edge projectionは派生curve上に置く | sag有効の2点route | source_edge_projection_world | Boundary: saved span bindingからcurveを解決し、chord lerpではなくcurveの同一parameterを評価する | SavedBackboneGraph / curve cache | 途中分岐が既存cableから浮く回帰防止 |
| C666 | Boundary | backbone | 世代を跨ぐterminal continuationをpair identityで接続する | 2点route後に末端から延長 | GenerateFromBackboneSpec | Boundary: 別portでもsaved pair rowがincident edgeを結ぶ場合はNodePatchCurveを生成する | SavedBackbonePortBinding.row_key / visual curve parts | point proximityや同一port偶然一致へ依存する回帰防止 |
| C667 | Boundary | backbone | branch追加後もthrough patchを維持する | 3点routeの中央からbranch追加 | GenerateFromBackboneSpec | Boundary: 3 incident endpointの中からconnectivity-owned pairだけをpatch化する | pair row / visual curve parts | group size 3で既存through丸めが消える回帰防止 |
| C697 | Invariant | backbone | saved edge は lateral offset 生成入力を echo 保存する | lateral_offset_m=+1 の2点route / 未指定 / fresh state 2つ | GenerateFromBackboneSpec | Invariant: saved edge の lateral_offset_m は +1.0、未指定は0.0、同一入力freshで決定的 | SavedBackboneEdge.lateral_offset_m | 保存済み graph から復元できない placement constraint を regenerate 前処理で使えるようにする |
| C668 | Invariant | backbone/update | regenerate fixed count increase は saved lateral offset を消費する | `lateral_offset_m != 0` の2点route + count 1→2 | GenerateFromBackboneSpec→UpdateBundleTemplate | Invariant: fresh lateral count2 と port/curve が一致する | SavedBackboneEdge.lateral_offset_m / CurveCacheEntry | placement echo を使わず既定 offset で増加再生成する回帰防止 |
| C669 | Invariant | backbone/update | multi-bundle regenerate は count 減少も対象 bundle だけを変え fresh と一致する | LV2+Communication の2点route + LV count 2→1 | GenerateFromBackboneSpec→UpdateBundleTemplate | Invariant: LV port/curve は fresh LV1+Communication と一致し、Communication の span/port id と位置は不変 | SavedBackboneGraph / Span / Port / CurveCacheEntry | multi-bundle count 減少で非対象 bundle を動かす、または対象 bundle の group offset を誤る回帰防止 |
| C670 | Invariant | backbone/update | regenerate は pair row を含む3点routeを fresh と一致させる | 3点route + count 1→2 | GenerateFromBackboneSpec→UpdateBundleTemplate | Invariant: pair row を含む全 edge bundle の port/curve が fresh LV2 と一致する | pairs / row placement / CurveCacheEntry | pair row を regenerate 側で再解釈し、中間共有 port を分裂させる回帰防止 |
| C671 | Boundary | backbone/update | regenerate は pipeline stage を再利用し専用 emit を持たない | regenerate source | source guard | Boundary: regenerate source は saved-scope `build_input` を作って `build` を呼び、`AddPort` / `AddSpan` / rules 手組み / saved graph node-edge 書き換えを持たない | pipeline stage / SavedBackboneGraph | M1 regenerate が第2 pipeline へ戻る回帰防止 |
| C672 | Invariant | backbone/update | regenerate は存続 lane の manual port を保持する | 2点route + port手動移動 + count 1→2 | GenerateFromBackboneSpec→MovePort→UpdateBundleTemplate | Invariant: count 増加は成功し、既存 manual port の id / world position / manual marker が維持される | port placement / manual override | 手編集済み port を regenerate が黙って上書きまたは拒否する回帰防止 |
| C673 | Invariant | backbone/update | regenerate は存続 span の user attachment を保持する | 2点route + lane0 span attachment + count 1→2 | GenerateFromBackboneSpec→AddAttachment→UpdateBundleTemplate | Invariant: count 増加は成功し、attachment id / span id / relation index が維持される | Attachment / relation index / Span | 存続 span のユーザー追加 attachment を regenerate が不要に拒否または喪失する回帰防止 |
| C698 | Invariant | backbone/update | regenerate fixed count decrease は退役 lane を削除し残存出力を fresh count1 と一致させる | LV count2 生成後に count1 へ減少 | GenerateFromBackboneSpec→UpdateBundleTemplate | Invariant: span/port/binding 退役、node/edge/edge_bundle id 不変、fresh count1 と port/curve が一致 | SavedBackboneGraph / Span / Port / CurveCacheEntry | count 減少を migration 複製ではなく reconcile 付き regenerate で扱う |
| C699 | Invariant | backbone/update | regenerate fixed count decrease は saved lateral offset を消費する | lateral_offset_m=+1 の LV count2 route を count1 へ減少 | GenerateFromBackboneSpec→UpdateBundleTemplate | Invariant: saved edge の lateral_offset_m は +1、fresh lateral count1 と一致し default fresh とは異なる | SavedBackboneEdge.lateral_offset_m / CurveCacheEntry | regenerate 前処理が placement echo を使わず既定 offset で再生成する回帰防止 |
| C700 | Boundary | backbone/update | regenerate fixed count decrease は退役対象 attachment を mutation 前に拒否する | LV count2 生成 + lane1 span に attachment + count1 減少 | AddAttachment→UpdateBundleTemplate | Boundary: unsupported で state 件数と template は不変 | Attachment / Span / SavedBackboneSpanBinding | ユーザー attachment 付き span を暗黙退役する回帰防止 |
| C701 | Boundary | backbone/update | regenerate は派生出力から topology を推測しない | regenerate source | source guard | Boundary: regenerate source は pipeline stage を呼び、layout/curve/visual/render/port world_position を topology 入力にしない | pipeline graph / SavedBackboneGraph | 差分再生成が span/layout/curve/port 位置から topology を復元する第2 pipeline へ逸れる回帰防止 |
| C702 | Invariant | backbone/update | regenerate fixed count は増減混在後も fresh count1 と一致する | LV count2 生成後に 2→3→1 | UpdateBundleTemplate | Invariant: 最終 span は1本、fresh count1 と port/curve が一致 | Span / Port / CurveCacheEntry | 増加だけ旧 migration、減少だけ regenerate という分岐を残す回帰防止 |
| C742 | Invariant | backbone/update | fixed count 減少は metadata rename と同時でも統一 regenerate を通る | LV count2 生成後に count1 + name変更 | UpdateBundleTemplate | Invariant: 更新成功後の span 数は1、template の count/name は編集値と一致 | BundleTemplate / Span | count 減少側だけ metadata 一致を要求し、増加側と受理範囲が分岐する回帰防止 |
| C743 | Boundary | backbone/update | BundleTemplate field diff の決定者は classifier 一箇所だけである | UpdateBundleTemplate source scan | wire_core_tests | Boundary: UpdateBundleTemplate は classifier を呼び、`it->second` の field 比較を持たない | classify_bundle_template_changes | routing block が field 条件を再列挙し、分類器と受理範囲が再び drift する回帰防止 |
| C744 | Boundary | backbone/layout | lowered support group key の収集は一箇所だけである | backbone source scan | wire_core_tests | Boundary: `append_group_key` は shared layout derivation の定義と2回の呼出しだけで、pipeline/derive は同じ関数を呼ぶ | derive_span_layout | generation と post-edit derive が group key/default/basis を別々に転記して drift する回帰防止 |
| C745 | Boundary | backbone/population | 旧population wrapとCableTemplate coil familyはproduction surfaceに残らない | source scan | wire_core_tests | Boundary: core、bindings、`web/src`、persistence、docsに旧wrap familyは無い。AttachmentInternalPathTemplateのcoilは対象外 | retired old family | 旧carrier-following wrapまたはCableTemplate coilが新assemblyと並存する回帰防止 |
| C746 | Invariant | backbone/generation | generation は isolated trial を成功時だけ move commit し、66 pole級stateで copy は総時間の20%以内 | 650m/10m interval の平行2 route | GenerateFromBackboneSpec x2 | Invariant: 1本目で66 pole以上を生成後、十分離れた2本目も66 pole以上を生成し、2本目の GenerationTiming.state_copy_ms / total_ms <= 0.20 | CoreState trial / GenerationTiming | 空stateだけを測ってcopy costを過小評価する、またはpipeline後半failureが本stateへ漏れる回帰防止 |
| C747 | Invariant | backbone/update | range count policy は保存済み conductor count を検証し、範囲内なら出力を再生成せず、範囲外なら対象 bundle を示して state 不変で拒否する | LV1 の2点route + range [1,3] / range [2,3] | UpdateBundleTemplate | Invariant: [1,3] は port/span identity と curve を維持して成功し、[2,3] は bundle id を含む error で拒否して template/output を維持する | Bundle.conductor_count / BundleTemplate count rule | range化を不要にregenerateする、または範囲外の既存 bundle を黙って不整合にする回帰防止 |
| C748 | Invariant | backbone/update | bundle policy差分はscopeをfresh等価へreconcileし、fresh-invalid入力はmutation前に拒否する | LV2 の3点route + Communication の別route + `default_spacing_m`変更 / `default_layer=Unknown` | GenerateFromBackboneSpec→UpdateBundleTemplate | Invariant: spacing変更後のLVはfreshと一致し別routeのdata_version/layout/curve/boundsは不変、Unknown layerはtemplate versionと両scope出力が不変 | BundleTemplate / SavedBackboneGraph / SpanLayoutEntry / CurveCacheEntry / BoundsCacheEntry | topology policyをrejectし続ける、scope外を再導出する、freshでは拒否される入力をpartial mutationする回帰防止 |
| C749 | Invariant | backbone/generation | Offset=0はband既定位置から追加移動せず、他bundleにも影響されない | LV単独route / LV+Communication route、HV 3lane route | GenerateFromBackboneSpec | Invariant: 同じLV endpoint位置が一致し、HV各laneは左・中央・右band identityと`lateral_center_m`を使用する | PortPlacementBand / SavedBackbonePortBinding / BackboneSpec.constraints.lateral_offset_m | bundle index由来の隠れた横offset、band中心無視、HV全laneの単一band展開への回帰防止 |
| C703 | Boundary | backbone/update | bundle count migration シンボルは core/src から消える | core/src source scan | source guard | Boundary: `migrate_backbone_bundle` / `regenerate_backbone_bundle_count_change` と旧 migration file が存在しない | source text | 差分別 migration operation が残る回帰防止 |
| C704 | Boundary | backbone/update | regenerate は post-edit API から直接呼ばれ update plan 経由で実行しない | pipeline/derive/regenerate source scan | source guard | Boundary: 旧 prepared pipeline entry が無く、regenerate は saved-scope `build_input` で `build` を呼び、`execute_update_plan` は loop 前に `kRegenerate` を拒否する | source text | pipeline 内に migration 語が残る、または plan が差分入力なしで regenerate 実行経路になる回帰防止 |
| C705 | Invariant | backbone/update | saved edge_bundle の順序は request bundle spec 順と一致する | LV+Communication の2点route生成 | GenerateFromBackboneSpec | Invariant: SavedBackboneEdgeBundle 配列順が request bundles の順序と一致する | SavedBackboneEdgeBundle / Bundle | multi-bundle regenerate が group offset を保存済み edge_bundles 順から復元する前提を固定 |
| C706 | Invariant | backbone/update | multi-bundle regenerate は対象 bundle だけを増減し fresh と一致する | LV+Communication の2点route + LV count 1→2 | GenerateFromBackboneSpec→UpdateBundleTemplate | Invariant: LV port/curve は fresh LV2+Communication と一致し、Communication の span/port id と位置は不変 | SavedBackboneGraph / Span / Port / CurveCacheEntry | multi-bundle group offset を単一bundleとして再生成したり、非対象 bundle を動かす回帰防止 |
| C707 | Invariant | backbone/update | saved edge の route/order から3点route順を復元できる | 3点route生成 | GenerateFromBackboneSpec | Invariant: 2 edge は同一 route、order 0/1、共有 node で連続する | SavedBackboneEdge.route / order / node ids | R2 regenerate が route_id echo を増やす前に既存保存入力で復元できることを固定 |
| C708 | Invariant | backbone/update | 3点route regenerate は count 減少後 fresh と一致し、scope外routeを触らない | LV2 の3点route + Communication の別route + LV count 2→1 | GenerateFromBackboneSpec→UpdateBundleTemplate | Invariant: 対象 edge bundle の port/curve と NodePatch 数が fresh LV1 の3点route と一致し、別routeの data_version/layout/curve/bounds は不変 | SavedBackboneGraph / SpanLayoutEntry / CurveCacheEntry / BoundsCacheEntry / VisualCurvePartCache | pair row を含む route-local regenerate が一部 edge だけを再生成する、またはscope外routeを再導出する回帰防止 |
| C709 | Invariant | backbone/update | 3点route regenerate は count 増加後 fresh と一致し、scope外routeを触らない | LV1 の3点route + Communication の別route + LV count 1→2 | GenerateFromBackboneSpec→UpdateBundleTemplate | Invariant: 対象 edge bundle の port/curve と NodePatch 数が fresh LV2 の3点route と一致し、別routeの data_version/layout/curve/bounds は不変 | SavedBackboneGraph / SpanLayoutEntry / CurveCacheEntry / BoundsCacheEntry / VisualCurvePartCache | 増加時に中間 pair row の共有 port を再利用せず edge ごとに分裂させる、またはscope外routeを再導出する回帰防止 |
| C710 | Invariant | backbone/update | multi-bundle 3点route regenerate は fresh と一致し、scope外routeを触らない | LV+Communication の3点route + Optical の別route + LV count 1→2 | GenerateFromBackboneSpec→UpdateBundleTemplate | Invariant: 対象 LV/Communication edge bundle の port/curve と NodePatch 数が fresh と一致し、別routeの data_version/layout/curve/bounds は不変 | SavedBackboneGraph / SpanLayoutEntry / CurveCacheEntry / BoundsCacheEntry / VisualCurvePartCache | R1 の group offset 復元と R2 の pair row 復元を同時に満たし、scope外routeを再導出しない |
| C711 | Invariant | backbone/update | regenerate count 減少は存続 lane の attachment を保持する | LV2 の2点route + lane0 span attachment + LV count 2→1 | GenerateFromBackboneSpec→AddAttachment→UpdateBundleTemplate | Invariant: count 減少は成功し、lane0 attachment id / span id / relation index が維持される | Attachment / relation index / Span | count 減少で存続 span の attachment まで拒否または削除する回帰防止 |
| C712 | Invariant | backbone/update | cable decision 差分は regenerate で fresh と一致する | LV の3点route + CableTemplate continuity 変更 | GenerateFromBackboneSpec→UpdateCableTemplate | Invariant: decision 更新は成功し、curve requested policy と port/curve/NodePatch が編集後 fresh 生成と一致する | CableTemplate / CurveCacheEntry / VisualCurvePartCache | decision 差分を stale 成功や direct derive に逃がさず統一 regenerate へ通す |
| C738 | Invariant | backbone/update | default endpoint attachment 差分は auto endpoint だけを reconcile する | LV route のdefault endpointをAで生成 + user attachment + Communication の別route + A→B | GenerateFromBackboneSpec→AddAttachment→UpdateCableTemplate | Invariant: auto endpoint はBのfresh生成と同値、user attachment はid/span/template/t不変、別routeのdata_version/layout/curve/boundsは不変 | CableTemplate / Attachment / SpanLayoutEntry / CurveCacheEntry / BoundsCacheEntry | default endpoint差分を未消費またはuser attachmentまで置換し、scope外を再導出する回帰防止 |
| C713 | Invariant | backbone/update | pole type structural 差分は regenerate で fresh と一致する | LV の3点route + PoleType band side/lateral 変更 | GenerateFromBackboneSpec→UpdatePoleTypeDefinition | Invariant: structural 更新は成功し、port/curve/NodePatch が編集後 fresh 生成と一致する | PoleTypeDefinition / SavedBackboneGraph / CurveCacheEntry / VisualCurvePartCache | pole template 構造差分を reject や stale 成功にせず統一 regenerate へ通す |
| C714 | Boundary | backbone/update | regenerate は退役 lane の manual port を mutation 前に拒否する | LV count2 route + lane1 port手動移動 + count 2→1 | GenerateFromBackboneSpec→MovePort→UpdateBundleTemplate | Boundary: 退役対象 manual port がある count 減少は unsupported で state count と template が不変 | port placement / manual override / SavedBackboneSpanBinding | 手編集済み port を退役時に暗黙削除する回帰防止 |
| C715 | Invariant | backbone/update | backbone span branch-down override は regenerate で layout/curve に反映する | HV の2点route + branch_down override + clear | GenerateFromBackboneSpec→SetSpanBranchDownOffsetOverride→ClearSpanBranchDownOffsetOverride | Invariant: override 設定は成功し、layout の branch_down_offset と curve が変わり、clear 後は override 値から戻る | override_state / SpanLayoutEntry / CurveCacheEntry | backbone span override を拒否または保存だけして派生出力を stale にする回帰防止 |
| C716 | Invariant | backbone/update | backbone span endpoint socket override は regenerate で layout に反映する | LV の2点route + start endpoint socket override + clear | GenerateFromBackboneSpec→SetSpanEndpointSocketOverride→ClearSpanEndpointSocketOverride | Invariant: override 設定は成功し、layout endpoint source と resolved socket が変わり、clear 後は自動状態へ戻る | override_state / SpanLayoutEntry | socket override を非backbone専用の保存状態に閉じ込める回帰防止 |
| C739 | Invariant | backbone/update | span override regenerate は別routeを触らない | 独立2route生成後、片方のspan branch-down overrideを設定 | SetSpanBranchDownOffsetOverride | Invariant: 別routeの edge_bundle span/port identity、span data_version、layout endpoint、curve samples、bounds が不変 | SavedBackboneGraph / SpanLayoutEntry / Port / CurveCacheEntry / BoundsCacheEntry | span-local override が全route regenerate や雑なscope収集へ広がる回帰防止 |
| C740 | Invariant | backbone | geomで構築済みのbase curveをvisual curve partsが優先再利用する | 3点routeの単一bundle fresh生成 + 同span idの注入curve | GenerateFromBackboneSpec / make_visual_curve_parts | Invariant: statsのsections/curve_buildsは生成span数に一致し、visual bodyは注入した構築済みcurveをcacheや再buildより優先する | geom.curves / VisualCurvePartCache.stats | pipelineとvisualが同じbase spanのfull curveを二重構築する回帰防止 |
| C741 | Invariant | backbone | scoped visual curve rebuild は全量再構築と一致する | 2点route後に終端から1本延長 | GenerateFromBackboneSpec x2 | Invariant: scoped cache parts は同一stateの全量再構築partsとcanonical順で一致し、curve_buildsは新規span+1-hop隣接spanの2回 | VisualCurvePartCache.parts / stats | visual curve parts scoped更新が古いpartを残す、または全量N本を再構築する回帰防止 |
| C717 | Invariant | backbone/update | layout settings 差分は regenerate で fresh と一致する | LV の3点route + side scale 変更 | GenerateFromBackboneSpec→UpdateLayoutSettings | Invariant: layout settings 更新は成功し、編集後 settings での fresh 生成と curve/port signature が一致する | LayoutSettings / SavedBackboneGraph / CurveCacheEntry | layout settings 差分を部分 derive や stale 成功に逃がさず統一 regenerate へ通す |
| C727 | Boundary | backbone/update | pipeline execution entry は build(build_input) だけにする | pipeline source guard | wire_core_tests | Boundary: pipeline は旧 `run` / prepared regenerate entry / template override input を持たず、通常生成と saved-scope 再生成の違いを `build_input` に明示して共通 `build` へ渡す | pipeline::build / pipeline::build_input | regenerate が第2 pipeline 化し、通常生成と別stage列を持つ回帰防止 |
| C728 | Boundary | backbone/update | pipeline は run mode / skip flag を持たない | pipeline source guard | wire_core_tests | Boundary: pipeline header/source に `run_mode` / `mode_` / `include_new_poles` / `run_preflight` / `ready_` が無く、既存port更新と path index bind はデータ条件で行う | pipeline::build_input / pipeline::emit_ports / pipeline::save_graph | operation固有条件や検証skipが pipeline mode 分岐として復活する回帰防止 |
| C729 | Boundary | backbone/update | regenerate source は pipeline output と退役を手組みしない | regenerate/pipeline source guard | wire_core_tests | Boundary: regenerate.cpp は AddPort/AddSpan/rules/layout/cache_span_curve/execute_update_plan/retire_from_trial/remove_span_from_caches を持たず、退役実体は pipeline::retire_untouched にある | regenerate_backbone_edge_bundles / pipeline::build / pipeline::retire_untouched | regenerate.cpp が第2 pipeline や fallback 更新経路、退役ループを持つ回帰防止 |
| C718 | Boundary | backbone | viewer hit world height は source-edge branch の正本ではない | source edge segment pick の hit z を極端値にする | ResolveBranchPick→GenerateFromBackboneSpec | Boundary: source identity が保存され、support/path position は segment endpoint 高さ、layout endpoint は source projection になる | ResolveBranchPickResult / SavedBackboneNode.source_edge / SpanLayoutEntry | viewer preview 座標や source projection を支持点高さとして固定する回帰防止 |
| C719 | Invariant | backbone/update | source-edge branch endpoint は current curve projection に追従する | source-edge branch 作成後に sag を有効化 | GenerateFromBackboneSpec→UpdateGeometrySettings | Invariant: source curve projection が変わり、branch endpoint world も同じ projection へ更新される | SpanLayoutEntry / CurveCacheEntry / SavedBackboneNode.source_edge | source-edge branch endpoint が作成時 port 位置に stale 固定される回帰防止 |
| C720 | Boundary | backbone | source-edge pipeline 前半は curve projection を読まない | source scan | wire_core_tests | Boundary: `pipeline::check` と `emit_ports` は `source_edge_projection_world` / `CurveCacheEntry` / `EvaluatePosition` を読まず source binding だけを確認する | pipeline source / SourceEdgeProjectionRef | topology/port materialization 段が source cable geometry を必須入力に戻す回帰防止 |
| C721 | Invariant | backbone/update | source-edge identity は projection update 後も残る | source-edge branch 作成後に sag 更新 | GenerateFromBackboneSpec→UpdateGeometrySettings | Invariant: SavedBackboneNode.source_edge と endpoint source_projection ref が更新後も edge/t/bundle/lane を保持する | SavedBackboneNode / SpanLayoutEntry.source_projection | projection 更新を world height 保存へすり替える回帰防止 |
| C722 | Boundary | backbone | unresolved source-edge reference は mutation 前に失敗する | 不完全な pending source-edge support node | GenerateFromBackboneSpec | Boundary: unsupported で失敗し、pole/port/span/bundle/saved graph count が変わらない | EditResult.error / object counts | source identity 不解決時に途中まで entity を作る回帰防止 |
| C723 | Invariant | backbone | source-edge branch は source sag を変えない | sag 有効の source edge へ branch 追加 | GenerateFromBackboneSpec | Invariant: source span curve sample と sag amplitude は branch 追加前後で同一 | CurveCacheEntry | branch 追加を source cable 物理荷重として循環させる回帰防止 |
| C724 | Invariant | backbone/update | source template sag 変更は branch projection を更新する | source-edge branch 後に cable template sag_factor を変更 | UpdateCableTemplate | Invariant: source projection が変わり、branch endpoint world が current projection と一致する | CableTemplate / SpanLayoutEntry / CurveCacheEntry | sag/template 更新で branch endpoint が stale になる回帰防止 |
| C725 | Invariant | backbone/update | source layout settings 変更後も branch projection は current のまま | 3点source edge中間branch後に layout settings を変更 | UpdateLayoutSettings | Invariant: 更新は成功し、branch endpoint world が current projection と一致する | LayoutSettings / SpanLayoutEntry / CurveCacheEntry | layout更新で source-edge branch endpoint が stale になる、または ownerless source-edge route regenerate が拒否される回帰防止 |
| C726 | Boundary | backbone | source-edge branch projection は prior curve cache を要求しない | source span layout を残して CurveCacheEntry を消し、source-edge pending node から branch 作成 | GenerateFromBackboneSpec | Boundary: source identity と保存済み layout から current curve projection を解き、branch endpoint world が source projection と一致する | SourceEdgeProjectionRef / SpanLayoutEntry / CurveCacheEntry | pipeline 後半が prior curve cache に依存し、一発生成や cache 欠落時に branch endpoint を解けない回帰防止 |
| C674 | Boundary | backbone/placement | backbone port の band は saved binding、非 backbone port は共有 selector が決める | production source | source guard | Boundary: band priority 選択は共有 selector と generation selector の2定義以下、population は identity/distance で再選択しない | SavedBackbonePortBinding / port placement | 下流が band identity を再推測する回帰防止 |
| C675 | Boundary | backbone/placement | port layout yaw は saved binding を読み debug record を決定入力にしない | `effective_port_layout_yaw_deg` source | source guard | Boundary: production yaw resolver に `pole_orientation_debug_records` 読みがなく、binding の `layout_yaw_deg` を消費する | SavedBackbonePortBinding / port layout | debug 観測値が production 配置を決める回帰防止 |
| C676 | Invariant | backbone/placement | generation と post-edit は同じ saved layout yaw を使う | 3点route生成直後の全 pole に同一 transform の MovePole | GenerateFromBackboneSpec→MovePole | Invariant: 全 backbone port の world_position が double 完全一致 | SavedBackbonePortBinding / Port.world_position | generation と endpoint refresh の yaw 決定者が分岐する回帰防止 |
| C677 | Boundary | backbone/placement | corner side scale と inner side 判定は1定義だけを持つ | core/src production source | source guard | Boundary: `apply_corner_side_scale` と `inner_side_for_turn` の定義数は各1 | port placement | 同じ角補正式が state/template/refresh で分岐する回帰防止 |
| C678 | Invariant | template/model | ModelDescriptor merge は決定的で conflict を補完しない | measurement + override | merge | Invariant: 有効な socket 位置 override は適用し、消えた socket 参照は conflict に載せて無視し、入力を変更しない | ModelMergeResult | import/adapter 前に descriptor 契約を pure logic として固定し、fallback 補完や state 依存へ流れる回帰を防ぐ |
| C679 | Invariant | template/model | 使用中 AttachmentTemplate の幾何差分は curve を再導出する | backbone span + attachment + socket位置変更 | AddAttachment→UpdateAttachmentTemplate | Invariant: 幾何のみ差分は成功し、対象 span の replacement path points が更新される | CurveCacheEntry / AttachmentTemplate | モデル再読込の socket 位置変更が全拒否され、手動で置いた attachment の曲線が stale になる回帰を防ぐ |
| C680 | Boundary | template/model | 使用中 AttachmentTemplate の構造差分は mutation 前に拒否する | backbone span + attachment + socket追加 | AddAttachment→UpdateAttachmentTemplate | Boundary: socket増加は structural unsupported として拒否し、template と curve は不変 | AttachmentTemplate / CurveCacheEntry | socket identity 変更を geometry 更新として通し、既存 attachment の接続意味を壊す回帰を防ぐ |
| C681 | Invariant | template/model | path無し Replace は hidden interval のみ生成する | ReplaceWithInternalPath template から internal path を除去 + attachment | AddAttachment→DeriveGeneratedSpanOutputs | Invariant: hidden interval は生成され、points空の replacement path は作らない | DetailCurve.hidden_intervals / replacement_paths | モデル本体が区間を埋める前提の attachment を internal path 必須に戻す回帰を防ぐ |
| C682 | Boundary | Validate | 同一 span の replaced interval 重複は Error とする | 同一 span に重なる Replace attachment 2個 | Validate | Boundary: overlap は Error issue を出し、補完や自動シフトをしない | ValidationIssue | 置換区間同士が重なって線の可視区間が曖昧になる回帰を防ぐ |
| C683 | Invariant | template/model | ModelDescriptor から path無し AttachmentTemplate を生成する | line_in/line_out socket を持つ descriptor | build_attachment_template | Invariant: line socket は安定IDで template socket へ写り、internal path は生成しない | AttachmentTemplate | DCC marker descriptor を state や viewer に依存せず core template へ変換できることを固定する |
| C684 | Invariant | template/model | 端子函 descriptor の drop socket は補助 socket として保持する | line_in/line_out/drop socket を持つ descriptor | build_attachment_template | Invariant: drop socket は main line replace の対象にせず template socket として保持する | AttachmentTemplate.sockets | 追加 socket を topology や line path と誤解して補完生成する回帰を防ぐ |
| C685 | Invariant | template/model | descriptor-built template は既存更新経路で使用中 span を更新する | descriptor→AttachmentTemplate→既存 template id へ適用 + attachment | UpdateAttachmentTemplate | Invariant: path無し replace は hidden interval を作り、descriptor再読込の socket位置変更で curve が更新される | AttachmentTemplate / CurveCacheEntry | モデル再読込専用経路を作らず、通常 template 更新経路で使用中 attachment を更新する契約を固定する |
| C686 | Boundary | backbone population | BundleTemplate の population rule は派生線だけを追加する | population_rules 付き LowVoltage template + 2点 route | UpdateBundleTemplate→GenerateFromBackboneSpec | Boundary: extra EdgeBody が出るが Pole/Port/Bundle/Span/SavedGraph counts は変えない | BundleTemplate.population_rules / VisualCurvePartCache | 追加平行線を topology や saved graph として保存する回帰防止 |
| C687 | Boundary | backbone population | population rule 差分は kReshape として再導出する | 生成済み route + rule seed 変更 | UpdateBundleTemplate | Boundary: rule-only 変更は成功し、last_update_timing.kind が kReshape で extra visual が残る | UpdateKind / BundleTemplate.population_rules | rule 変更を regenerate 級として拒否したり topology_change に混ぜる回帰防止 |
| C755 | Invariant | backbone | 鋭角Jumperはincident spanへG1接続する | 120度折返しroute | GenerateFromBackboneSpec | Jumper両端sample接線が接続するEdgeBody sample接線と一致 | VisualCurvePart samples | 鋭角接続がport間chord接線で折れる既存不具合の固定 |
| C756 | Boundary | Persistence | write/readは型別wrapperを持たず同じarchive visitorから導出する | persistence.cpp source scan | wire_core_tests | 型別WRITE/READマクロとwrite_pole/read_pole等が存在しない | source text | field列挙一元化後にwrite/read二重familyが再発することを防ぐ |
| C757 | Invariant | Persistence | load後authoritativeは同じarchive field列挙で直接一致する | save/load roundtrip state | SerializeAuthoritative→DeserializeAuthoritative | CoreStateTestHook::authoritative_equalsがtrue | authoritative storage fields | writer/reader双方から同じfieldが落ちbyte一致だけ通る盲点を防ぐ |
| C758 | Invariant | backbone visual assembly | enabled assemblyはlogical spanごとにsupport pathとhelixを1本ずつ派生する | existing support band付きLV template | UpdateBundleTemplate→GenerateFromBackboneSpec | generated span数とkSupportPath/kHelix supplemental数が一致 | VisualCurvePartCache | Bundle全laneを一体化したりauthoritative topologyを増やす回帰を防ぐ |
| C759 | Boundary | backbone visual assembly | support primary curve・helix・containment・wander・twistはspan visual assemblyだけが決定する | owner / caller source scan | wire_core_tests | Boundary: span_visual_assembly.cppだけがsupport curveとassembly数式を持ち、curve_partsはlayout endpointを渡してownerを一度呼ぶだけ。近傍検索やauthoritative object生成は無い | source text | category別support実装やvisual assemblyをtopology／curve_partsへ分散させる回帰を防ぐ |
| C764 | Invariant | model assembly / Layout | straight HVはPole、saved row、Portのowner単位でmodelを派生しwire socketをcurve端点にする | body、mesh下端半径で正規化したcrossarm+belt、wire socket付きendpoint assemblyを登録したHV3相2点route | GenerateFromBackboneSpec→MovePole | body各Pole1、crossarm+belt各row1、endpoint fixture各一意Port1。support/endpoint/curve端点はsocket一致。belt final scaleは`placement radius / mesh lower-end radius`で、mesh下端配置では1。移動後もstable key維持・version更新 | VisualModelInstanceCache / SpanLayoutEntry / CurveCacheEntry / authoritative counts | Web後合わせ、laneごとの腕金重複、belt基準をground半径と混同する、fixture追加によるtopology増加、post-edit時のstale modelを防ぐ |
| C765 | Invariant | model assembly / Layout | branch loweringはfixture placementへ一度だけ適用し、最終model socketをcurve endpointにする | wire socket付きHV endpoint assembly、3点route中央からpass-through branch | GenerateFromBackboneSpecを2回 | lowered endpointは`support_world == endpoint_world == visible model socket == curve endpoint`。同一Portのdown offsetは一致し、全bound Portのfixture stable keyは一意 | VisualModelInstanceCache / SpanLayoutEntry / CurveCacheEntry | modelを元高さに残して線端だけ下げる、またはbranch追加で共有Port fixtureをspan endpointごとに二重生成する回帰を防ぐ |
| C766 | Invariant | model assembly / Placement / Layout | row mount・endpoint fixture・接続線は一つのendpoint placementを共有し、band lateral変更へ追従する。beltは柱軸に残る | mount socketとwire socket付きHV3相2点routeでassembly versionとPoleType bandを更新 | GenerateFromBackboneSpec→UpdateModelAssemblyTemplate→UpdatePoleTypeDefinition | crossarm model offsetはinsulatorとcurve端点へ伝わりstable keyは維持される。band変更時はrigid row fixture、Port fixture、curve端点が同量移動し、kPoleRadial beltはworld位置不変 | ModelAssemblyTemplate / ResolvedEndpointPlacement / VisualModelInstanceCache / SpanLayoutEntry / CurveCacheEntry | 腕金のmodel offsetだけが碍子・線へ伝わらない、腕金だけがband offsetへ追従しない、またはbeltまで柱から外れる回帰を防ぐ |
| C767 | Invariant | backbone visual assembly | default HV/LVのsupport pathはOpticalと同じprimary curve生成を使い、接続曲線境界で切れず解決済みendpointへ届く | HV3相3点route、LV1線3点route | GenerateFromBackboneSpec | spanごとに1本、両端はlayout endpointと一致し中央は線径分離。support 1本につきcurve build 1回で、helix・topology・member samplesは不変 | SpanVisualAssemblyTemplate / SpanVisualAssemblyEndpointMap / VisualCurvePartCache | EdgeBody複製によるnode patch境界での切断、カテゴリ別curve family、碍子socket無視、support-onlyでmember geometryを変える回帰を防ぐ |
| C769 | Invariant | backbone bundle placement | 同じBundleTemplateを複数配置してもBundle単位でidentity・絶対配置・node patch・中間分岐attachmentを分離する | Optical templateをpole-local高さ・横位置・spread違いで2回指定する3点routeから中間分岐 | GenerateFromBackboneSpec | Bundle/Spanは各2、両端Portのlocal座標差は指定値と一致し、各Bundleにsupport/helixがspan単位、node patchがBundle単位で存在し、分岐は各source Bundleのedge-bundleへ接続する | BackboneBundleSpec / SourceEdgeProjectionRef / Bundle / Port / VisualCurvePartCache | template id集合へ畳む、同templateの最初のsource attachmentを使う、support/helix/node patchだけ旧位置や別Bundleへ混ざる回帰を防ぐ |
| C770 | Invariant | backbone bundle placement / model assembly | Bundle placementのheight更新は交差routeのrow height offsetを保ったままPort・碍子・腕金・線端へ反映する | row assemblyとendpoint assembly付きHV routeへ交差HV routeを追加し、生成済みBundleのheightだけを更新 | GenerateFromBackboneSpec→UpdateBackboneBundlePlacement | 交差row offsetは更新後Bundle heightに対して維持され、各incident spanの`support_world == endpoint_world == visible model socket == curve endpoint`、Port平均高さとrow model高さは指定height差分だけ移動する | Bundle / Port / SpanLayoutEntry / CurveCacheEntry / VisualModelInstanceCache | placement更新時にrow height offsetまたはmodel socket補正を線端から落として、交差部の碍子・腕金と線が分離する回帰を防ぐ |
| C771 | Invariant | backbone graph | incremental cross completion は one-shot cross と同じ canonical pair row へ収束する | A-B-C 後に D-B-E を一括追加する状態と、A-B-C、B-D、E-B の段階追加状態を比較 | GenerateFromBackboneSpec x3 | B の saved row key は pair A-B-C と pair D-B-E の2つだけで open は残らず、B-D の既存Port/Span IDは維持され、NodePatch・Port位置・support level・curve endpoint は one-shot と一致 | SavedBackboneGraph / SavedBackboneRowKey / SavedBackbonePortBinding / VisualCurvePartCache / SpanLayoutEntry | degree four の saved context/current route 差で既存 open と新規 open が残り、3段目 row を生成する回帰を防ぐ |
| C772 | Boundary | backbone graph | incremental pair promotion は複数の saved open candidate を推測で選ばず mutation 前に拒否する | A-B-C、B-D、B-F 後に E-B を追加 | GenerateFromBackboneSpec x4 | 生成は `ambiguous promoted open edge` で失敗し、Pole/Span/SavedBackboneEdge/PortBinding 数は試行前から変わらない | SavedBackboneGraph / pair continuity | first-match や最小角度選択で誤った open row を pair 化する回帰を防ぐ |
| C773 | Invariant | backbone graph | sharp corner の incremental completion は pair 昇格せず open row と jumper 契約を維持する | A-B-C、B-D 後に B-D 方向に鋭角な E-B を追加 | GenerateFromBackboneSpec x3 | B は main pair と BD/BE の open rows を持ち、既存BD Port IDを維持し、curve endpoint は layout と一致 | SavedBackboneRowKey / jumper / SpanLayoutEntry | sharp corner を通常 continuation としてpair化し、dead-end row/jumper契約を壊す回帰を防ぐ |
| C774 | Invariant | backbone graph | bundle/port scope が違う incremental completion は既存Portを誤共有しない | LVのA-B-C、LVのB-D後にHVのE-Bを追加 | GenerateFromBackboneSpec x3 | BE側のB PortはBD側のLV Port IDと共有されず、BD側Port IDは維持される | SavedBackbonePortBinding / BundleTemplateId / PortKind / PortLayer / placement band | row keyだけで異scopeのport bindingを再利用する回帰を防ぐ |
| C775 | Invariant | Persistence / backbone graph | incremental canonical pair row は save/load 後も維持される | C771のincremental状態をSerializeAuthoritative→DeserializeAuthoritative | SerializeAuthoritative / DeserializeAuthoritative | load後のB row構成、Port位置、NodePatch数、fixture数、support level、curve endpoint が保存前と一致 | SavedBackboneGraph archive / derived rebuild | 保存形式やload再派生でcanonical pairがopenへ戻る回帰を防ぐ |
| C776 | Invariant | backbone regenerate | incremental canonical pair row は regenerate 後も維持される | C771のincremental状態で新規BE側Bundle placementを更新 | UpdateBackboneBundlePlacement | 更新成功後もBはcanonical pair構成で、curve endpoint は layout と一致する | SavedBackboneGraph / bundle placement regenerate | regenerateがsaved open由来のpairを3rowへ戻す回帰を防ぐ |
| C777 | Invariant | backbone graph | reverse input direction でも incremental completion は同じ canonical pair key へ収束する | A-B-C、B-D後にE-Bで追加した状態とB-Eで追加した状態を比較 | GenerateFromBackboneSpec x3 | 両状態ともBは2 pair / 0 openで、Port位置・NodePatch数・fixture数・support level・curve endpoint が一致 | SavedBackboneRowKey min/max edge ordering / pair axis | 入力方向によってpair keyやrow配置が反転する回帰を防ぐ |
| C778 | Invariant | backbone graph | multi-template の incremental completion は各互換scopeを1回だけ昇格し、別scopeや別openを曖昧扱いしない | LV/HV/Communication/OpticalのA-B-C、B-D後にE-Bを追加 | GenerateFromBackboneSpec x3 | Bは2 pair / 0 open、BD側Port/Span IDは維持され、BE側span数はBD側と一致し、curve endpoint は layout と一致する | SavedBackbonePortBinding / BundleTemplateId / SavedBackboneRowKey | pair rowの両端open bindingをscope外まで汎用探索して`ambiguous promoted open row binding`になる回帰を防ぐ |
| C779 | Invariant | backbone graph / bundle placement | same-template multi-placement の incremental completion は placement_key で既存Bundle/Portへ接続する | Web既定相当のHV1+LV3+Communication+OpticalでA-B-C、B-D後にE-Bを追加 | GenerateFromBackboneSpec x3 | HV 3相とLV/COMM/OPTを含む各placementは別placement_keyを持ち、B-D/E-Bは各8spanを生成し、BE側edge bundleはBD側の既存Bundle IDを使い、B側Portを共有し、Bは2 pair / 0 open、curve endpoint は layout と一致する | Bundle::placement_key / BackboneBundleSpec::placement_key / SavedBackboneEdgeBundle / SavedBackbonePortBinding | 同じBundleTemplateの複数placementをtemplate/kind/layer/band/laneだけで候補化して`ambiguous promoted open row binding`になる、またはHVを落とす/誤った高さのBundleへ接続する回帰を防ぐ |
| C780 | Invariant | backbone graph / bundle placement | 同じtemplate・同じ数値配置でもplacement_keyが違えば別placementとして扱い、request順序に依存しない | 同template・同height/offset/spacingの2placementでA-B-C、B-D後にE-Bだけ配置順を入れ替える | GenerateFromBackboneSpec x3 | BE側edge bundleはplacement_keyごとにBD側既存Bundle IDへ接続し、Bは2 pair / 0 open、curve endpoint は layout と一致する | Bundle::placement_key / BackboneBundleSpec::placement_key / SavedBackboneEdgeBundle | height/offset/spacingやrequest配列順でmatchingする回帰を防ぐ |
| C781 | Invariant | backbone graph | completed cross からさらにedgeを伸ばしても既存spanをretireしない | A-B-C、B-D、E-Bでcross completion後、B-Fを追加 | GenerateFromBackboneSpec x4 | B-D/E-Bの既存spanは残り、B-D側Port IDも維持され、新規B-Fはopen rowとして追加され、curve endpoint は layout と一致する | retire_untouched / SavedBackboneEdgeBundle / SavedBackboneSpanBinding | 既存Bundle IDを継続したedge bundleをcontext edge側までretire scopeに含め、元のspanを削除する回帰を防ぐ |
| C782 | Invariant | backbone graph | 複数sharp open候補があるjunctionへさらに鋭角edgeを追加してもpair promotion ambiguityでは失敗せず、新規edgeをopen rowとして追加する | A-B-C、B-D、E-Bでsharp open rowsを残した後、B-Gをさらに鋭角方向へ追加 | GenerateFromBackboneSpec x4 | B-G追加は成功し、Bは既存pair 1 / open 3を保ち、B-D/B-E/B-Gはopen rowのまま、B-D側Port IDを維持し、curve endpoint は layout と一致する | make(pairs) / is_promoted_open_continuation / sharp jumper rows | 複数sharp候補から推測で1本を選ばず、鋭角追加がambiguous promoted open edgeで失敗する回帰を防ぐ |
| C768 | Boundary | Persistence | 新field導入前のauthoritative stateは暗黙supportと既定Bundle placementを維持し現行形式へ移行できる | support_path_enabledとBundle placement行を除いた旧形式state | DeserializeAuthoritative→SerializeAuthoritative | load成功、helix有効bundleのみsupport有効、placementは0、resaveでfield復元 | BundleTemplate / Bundle authoritative archive | IndexedDBに残る旧workspaceのrestore不能化、旧helixから必須supportが消える回帰を防ぐ |
